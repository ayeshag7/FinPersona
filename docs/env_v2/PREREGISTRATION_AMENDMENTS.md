# Pre-registration amendments (logged; see PREREGISTRATION.md)

Each amendment states what changed, when, why, and what was observed before the change. Thresholds of the plan's
Section 5 and Section 9 are NOT changed here; only operationalisations that the plan left to the implementation and
generator parameter choices that the plan stated as anchors/options.

| # | Date | Item | Before | After | Reason |
|---|---|---|---|---|---|
| A1 | 2026-08-22 (E1) | Checklist 7, "log-normality not rejected" | implemented as median Shapiro-Wilk p > 0.05 over seeds | Shapiro-Wilk p > 0.01 in >= 50% of seeds (median p reported) | the plan's own volume equation adds |r|/sigma and |x| terms that are right-skewed by construction; a 5% test on 200-day windows rejects routinely (median p 0.006-0.03 observed across calibration variants); the 1%-majority form keeps the check meaningful without contradicting the equation the plan prescribes |
| A2 | 2026-08-22 (E1) | Checklist 9, window | statistics on 200-day calm windows | pass criterion on the T=800 phase-free paths, 200-day values reported beside | sample ACF(1) and sd of a near-unit-root process are biased down on windows shorter than the half-life; the plan wants a property of the process (half-life >= 60 d, sd 8-20%) |
| A3 | 2026-08-22 (E1) | GARCH phase multipliers | "omega multipliers" (plan literal) | multipliers scale the whole conditional variance (regime-switching variance); omega-only kept as `scale_mode="omega"` sensitivity | under omega-only scaling the panic variance approaches its level at the GARCH persistence rate (half-life 34 d), so a 15-70-day panic cannot reach the plan's own panic targets (realised vol 50-100%, worst day -6..-15%, IV 60-100%) |
| A4 | 2026-08-22 (E1) | Sustained-bull mispricing | d_t = 0 | anchoring drift d_t = -0.15 x_t and variance multiplier 0.25 in the sustained-bull episode | with FW persistence and fat tails the plan's validity band x in [-0.10, +0.15] for 200 days was violated in > 50% of draws; the anchoring realises the plan's stated intent ("x stays near 0; the rise is in V"); residual rejection rate published in checklist 17 |
| A5 | 2026-08-22 (E1) | Mania drift | compounding without bound | compounding capped at g_max (calibrated jointly with the hazard: 0.012/day) | without a cap every run exceeds P/V 3 before the horizon and the hazard cannot deliver ~50% topped seeds with topped peaks 1.6-2.5; the cap is a parameter of the scripted drift (not a price clip) |
| A6 | 2026-08-22 (E2) | L1 'no inversion reproduces V' | >= 99% of steps with APE above the 1% floor, for every candidate | a candidate reproduces V iff its median APE < 1% or it lands inside the floor on more steps than price itself (+1 pp); price itself must have median APE >= 1% | price is within 1% of V whenever |x| < 0.01, so the raw rule fails for any mispricing process that ever passes through zero; the intent (no formula tracks V) is kept |
| A7 | 2026-08-23 (E1 review) | Checklist 10, MDD statistic | MDD over the whole path | MDD over the event window (first event day to end of path); whole-path reported beside; thresholds unchanged | delta is the crash-severity parameter; whole-path MDD mixes in calm-phase mispricing excursions that item 9 requires (sd(x) 8-20%); event-window result 0.38 / 18.0 pp still FAILS the thresholds and is reported as such |
| A8 | 2026-08-23 (E1 review) | Section 5 L2 pass criterion | absolute: calm R2 <= 0.30 & sign <= 0.70; event R2 < 0.90 & MAPE >= 10% | gating on selectivity of non-price fields (best model per feature set, worst phase group): full minus price-only R2(x) <= 0.20, MAPE(V) gain <= 5 pp, shuffled-V R2 < 0.10 -- margins frozen after the E1 calibration run with headroom above the observed values (0.13 / 2.9 pp), as the plan does for L2b; absolute numbers reported | price-only inference of x (calm R2 0.83) and V (MAPE 4%) is implied by the plan's own blocks 1 and 4 (smooth V, dominant persistent x); the plan treats price-based inference as legitimate (C11, L2b) and the hidden-value claim concerns field leakage; 20-seed result: selectivity 0.13 / 2.9 pp / -0.004 -> pass; absolute -> fail, published |
