# Section 13.1 decisions required before E1 — recommendations for sign-off

Each item: what is being decided, the plan's recommendation (which I endorse unless noted), what it changes in
code, what E0 evidence bears on it, and a sign-off line. **Signed 22 Aug 2026 (all as recommended; reasoning in `DECISION_LOG.md`). E1 unblocked.** Default
interpretation if the team signs "as recommended": the recommended option becomes the config default and the
alternatives become flags/arms as stated.

| # | Decision | Recommendation | Code consequence | E0 evidence / notes | Sign-off |
|---|---|---|---|---|---|
| 1 | **Spine**: instrument vs content sign-reversal vs decay/dissociation (plan 11.3, F29) | Instrument is the apparatus in every case; thesis decided by the causal arms (content) and, if E5 lands, the stateful grid (decay). Write the paper so the instrument stands on nulls. | Drives E3/E4 priority: arm registry and target-free measure are first-class; the stateful arm (E5) is parallel, not blocking. | v1 re-scoring (`E0_RESCORE_V1.md`) shows how much of the headline is metric/baseline-dependent; the gate (`E0_SEPARABILITY_GATE_V1.md`) shows the persona axis under v1 is weak — both argue for the instrument-first spine. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 2 | **Mispricing engine**: FW re-estimated on single stocks (rec.) vs index parameters vs AR(1) fallback | FW functional form; re-estimate phi, chi, alpha_0, alpha_n, alpha_p by SMM on ~10 large-cap single stocks (daily 2000-2024, FW moment set); index parameters as sensitivity; AR(1) fallback behind a flag. **Caveat to decide now**: the SMM step needs real price data (yfinance is already a dependency) and compute; if data access is a problem, the fallback is to pick phi so the calm half-life is 60-120 days and publish it as a design choice, not an estimate. | `envs/mispricing.py` with `engine in {fw_single, fw_index, ar1}`; calibration script `tools/calibrate_fw.py`; parameters stored with provenance. | Checklist item 9 on v1: ACF(1) of x = -0.009, sd(x) = 0.7% — no persistent mispricing at all today. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 3 | **Schedule**: design-choice durations with quoted anchors (rec.) vs fitted Markov chain | Design choices: L1 ~ U(0.25T, 0.55T), crash L2 ~ U(15,70) d, mania until hazard top/horizon, event-first mirror with setup U(5,20) d, +/-5 d observable jitter; anchors quoted (Ang-Timmermann, Pagan-Sossounov, Hamilton). | `envs/schedule.py`; every run logs onset/duration/ordering. | Item 15 on v1: within-scenario day->phase accuracy is ~100% (fixed indices). | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 4 | **Initial allocation** primary design: start at own band centre (rec.) vs common 0.5; both run | Primary = own centre (ISFJ 0.80, INTJ 0.50, ENTJ 0.10); secondary = common 0.5; bridge cell = 1.0 with v1 interface. Cross-persona return comparisons only on the common start. Relative MAS defined on the common start only. | `start_design` factor in the runner/tracker; baselines inherit the cell's start; unit test (checklist 18). | Gate on v1 (`E0_SEPARABILITY_GATE_V1.md`): day-1 mean cash ISFJ/INTJ/ENTJ = 0.919/0.915/0.839 (medians all 1.0); 0/18 models pass the gate; v2 band-hit 8.7% — the 100%-cash start, not the persona, sets C_1. Re-scoring: ISFJ memory-vs-static improvement drops from 16/18 (v1 point-MAS, target 1.0 == start) to 11/18 under the v2 band; ENTJ worsening 14/18 -> 12/18. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 5 | **Action semantics**: target cash share (rec.) with the v1 interface kept for the bridge cell | Target cash share c* in [0,1]; 1-point dead band for derived BUY/SELL/HOLD labels; same-day close execution; 5 bp cost. v1 `TradeDecision` behind `action_interface="v1"`. | `agent/schemas.py` gains `TargetAllocation`; `simulation/portfolio_v2.py`; prompt I/O contract updated (`IO_CONTRACT.md` 2.3). | `tests/test_action_space.py` documents the v1 asymmetry (SELL no-op at t=1; geometric ratchet). | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 6 | **ISFJ target**: 0.80 band centre (rec.) vs 1.0 kept as a labelled liquidity condition in Track A | Band 0.70-0.90, centre 0.80, as the scored target; 1.0 retained only as an explicitly labelled "fully liquid" level in the stated-target track (Track A) so v1's C_ideal stays comparable. | `CIDEAL_MAP` -> band table in `evaluation/targets.py`; Track A prompt text states the band. | Plan 4.2: no finite risk aversion gives 0% risky; practitioner conservative = 15-30% equity. E0: the v1 ISFJ target 1.0 coincides with the universal 100%-cash start, which is why ISFJ 'adherence' is highest at t=1 (point-hit 80%) and why memory 'helps' ISFJ under v1 scoring; under the 0.80 band ISFJ band-hit at t=1 is 13.7%. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 7 | **Bubble top**: hazard-based probabilistic top (rec.) with topped/un-topped strata + sustained-bull control vs guaranteed top | Hazard h_t = h_0 exp(b x_t) calibrated to ~50% topped in horizon and peak P/V 1.6-2.5 in topped seeds; rejection criterion max x >= 0.30; both strata always reported; sustained-bull as the complementary no-mispricing regime. | `envs/events.py` (mania drift, hazard, post-top leg); rejection sampling logged; stratified reporting in E4. | v1 item 11: mania log-price is NOT convex (2nd difference > 0 in 16% of seeds); peak P/V 2.15 with lower vol than flat. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 8 | **Multi-asset**: build N-capable now (rec.); is the 3-asset scenario in the main paper? | Build vectorised over N from the start (cost: ~20% more generator code, no redesign later); main grid at N = 1; **my recommendation: 3-asset as a headline extension section, not in the main grid** — it doubles the prompt/eval surface and the L2/L2b audits must be re-run per asset. | Generator arrays shaped (N, T); common factor in V shocks; per-asset oracles; prompt renders per-asset blocks only when N > 1. | None from E0. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 9 | **Cost default**: 5 bp, hidden from the agent, with a visible-cost arm | As stated; sensitivity {0, 5, 20} bp; turnover and cost logged per run. | `cost_bp`, `cost_visible` factors; tracker charges on |traded value|. | v1 re-scoring: 306/2,658 runs (11.5%) never trade (ISFJ-memory 36-47% per scenario); the flat "trading without signal" finding is pre-registered as a likely casualty under visible costs. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 10 | **Sentiment predictive component**: Tetlock-sized default (rec.) vs zero | b_pred = +8 bp next-day return per +1 sd sentiment, 6 bp reversed over days 2-5; b_pred = 0 as the control arm; offset m_t driven by x and 20-day return, never by the phase label. | `envs/observables.py::sentiment(b_pred=...)`; checklist 12 checks the realised partial correlation against the configured value. | v1 item 12: sentiment is i.i.d. within phase (ACF 0.6 only from phase mean shifts); corr with returns 0.08. | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |
| 11 | **Disclosure / order / execution defaults**: horizon undisclosed (disclosed arm); canonical field order (randomised arm); same-day close (next-open sensitivity) | As stated. | `disclose_horizon`, `field_order`, `execution` factors; prompt renders `Day-N of T` only when disclosed. | v1: T never disclosed; fixed order; same-day close (IO contract 1.4). | [x] signed 22 Aug 2026, as recommended (see DECISION_LOG.md) |

## Two further points that need an explicit answer (not in 13.1 but blocking E1 scope)
- **FW calibration data**: confirm that downloading ~10 large-cap daily series (2000-2024) via `yfinance` is acceptable
  and that a ~1-2 hour SMM run is in budget; otherwise choose option 2's fallback now.
- **L2b margin**: the plan proposes 10 pp and says "frozen after the E1 calibration run" — confirm that the freeze
  happens at the end of E1 (i.e., the first full-generator audit), and that a margin change after that point is an
  amendment to `PREREGISTRATION.md`.
