# Decision log — Section 13.1 sign-off (22 Aug 2026)

Team instruction (22 Aug 2026): "do the recommended in each but note down your decisions and reasoning for each
somewhere". All eleven decisions in `DECISIONS_13_1.md` are therefore signed **as recommended by the plan**, with the
reasoning below. These choices are now the pre-registered defaults (`PREREGISTRATION.md`); the alternatives remain
configurable arms/flags as stated. Any later change is an amendment (`PREREGISTRATION_AMENDMENTS.md`).

| # | Decision | Chosen | Reasoning (why this and not the alternative) |
|---|---|---|---|
| 1 | Spine | **Instrument as apparatus; thesis decided by the causal arms (content) and, if E5 lands, the stateful grid (decay)** | Both v1 theses are fragile on the E0 evidence (gate 0/18, bidirectional ISFJ effect start-dependent, RG envelope degenerate); the instrument is the only component that stands under nulls and it is common to all three theses, so optimising the environment for it loses nothing for the others. |
| 2 | Mispricing engine | **FW functional form, parameters re-estimated on single stocks (SMM); index parameters as sensitivity; AR(1) behind a flag** | Index parameters give a ~580-day half-life that makes a 200-day benchmark nearly mispricing-free in calm phases; an AR(1) has no provenance for the overshoot/clustering the bull and crash regimes need. Re-estimation keeps the published functional form and makes the persistence an estimate, not a constant. **Operational rule adopted:** `tools/calibrate_fw.py` downloads ~10 large-cap daily series with `yfinance`; if the download fails in this environment (no network) or the SMM does not converge, the fallback is phi set so the calm half-life is 60-120 days, published as a design choice with the index parameters as the sensitivity — logged as such, never presented as an estimate. **[Phase 0 correction, 29 Aug 2026: the fallback in force (`fw_fallback_hl150`) has a pull-rate half-life of 150.0 d, φ = 0.4632 — outside the 60–120 d window stated here; `v2_1/PHASE_0_REPORT.md`.]** |
| 3 | Schedule | **Design-choice durations with quoted anchors** | A fitted Markov chain on index regimes would import index-level durations (years) that a 200-day horizon cannot contain, and would still be a modelling choice; stating the durations with their anchors is more transparent and keeps onset/duration a controlled, logged factor. |
| 4 | Initial allocation | **Primary: start at own band centre; secondary: common 0.5; bridge: 1.0 with the v1 interface** | E0 shows C_1 is set by the 100%-cash start, not by the persona (median 1.0 everywhere; 0/18 gate). Starting at the persona's centre removes the ratchet artefact on adherence; the common-0.5 design is needed for cross-persona return comparisons and for the gate; the bridge cell is the only way to compare with v1 at all. |
| 5 | Action semantics | **Target cash share with 1-pt dead band; v1 interface behind a flag for the bridge cell** | The v1 BUY/SELL asymmetry makes SELL a no-op at t=1 and makes any allocation reachable only from above; a target share is the minimal interface under which every allocation is reachable in one step and labels are derivable for RG. Keeping v1 behind a flag is required by the A1 x A2 factorial. |
| 6 | ISFJ target | **0.80 band centre (band 0.70-0.90); 1.0 only as a labelled liquidity condition in Track A** | No finite risk aversion yields 0% risky (plan 4.2); every practitioner anchor puts "conservative" at 15-30% equity; E0 shows the v1 target 1.0 coincided with the universal start and manufactured ISFJ's apparent adherence and its memory benefit. |
| 7 | Bubble top | **Hazard-based probabilistic top with topped/un-topped strata + sustained-bull control** | A guaranteed top is a clip of the kind the review objects to (v1's 1.05x floor) and removes the un-topped population in which caution is right but unrewarded in-horizon; the hazard gives a published conditioning and two honest strata; sustained-bull supplies the no-mispricing control the mandate-conflict question needs. |
| 8 | Multi-asset | **Build N-capable now; main grid at N = 1; 3-asset as a headline extension section (not in the main grid)** | Vectorising now costs little and avoids a rewrite; running the main grid at N = 1 keeps v1 comparability; putting 3 assets in the main grid would double the prompt/evaluation surface and require per-asset leakage/phase-clock audits before any headline number exists. |
| 9 | Cost | **5 bp per trade, hidden by default, visible-cost arm; sensitivity {0, 5, 20}** | 5 bp matches the S&P 500 bid-ask estimate and the institutional median; hidden-by-default preserves comparability with v1 (no cost was shown), and the visible arm tests the pre-registered casualty that the flat "trading without signal" result may invert under visible costs. |
| 10 | Sentiment predictiveness | **Tetlock-sized default (+8 bp/sd next day, 6 bp reversed over days 2-5), b_pred = 0 as control** | A zero default would make sentiment pure noise and invite the objection that the field is decorative; a documented, small, reversing effect is the empirically anchored choice and is trivially switchable; the control arm measures what the agent does with a non-informative field. |
| 11 | Disclosure / order / execution | **Horizon undisclosed (disclosed arm); canonical order (randomised arm); same-day close (next-open sensitivity)** | Each default preserves comparability with v1 (which used exactly these), and each alternative is a cheap arm that tests whether headline effects survive design choices that were previously constants. |


**SMM attempt (22 Aug 2026, `tools/calibrate_fw.py`):** yfinance download succeeded (AAPL, MSFT, JNJ, XOM, JPM, PG, KO,
WMT, GE, IBM, 2000-2024). Empirical nine-moment vector: ACF1(r) -0.047, mean|r| 1.15%, Hill 2.82, ACF|r| at lags
1/5/10/25/50/100 = 0.28/0.24/0.22/0.16/0.13/0.10. Nelder-Mead from the index set ended at J = 408 with the parameters
essentially unchanged (phi 0.114, chi 1.52, alpha_0 -0.33, alpha_n 1.82, alpha_p 18.5): the FW return moments barely
respond to the mispricing parameters once n_f ~ 1, and the long-memory of |r| in single stocks is a GARCH property,
not an FW one. **Not accepted** (`envs/v2/params/fw_single_stock.REJECTED.json`); the documented fallback (phi for a
~150-day stationary / ~70-day realised half-life) remains in force and is reported as a design choice. The empirical **[Phase 0 correction: 150 d is the pull-rate half-life ln2/(μ n̄ φ) (CAL); the long-pilot ACF(1) half-life is 147 d (five 200,000-step pilots, 141–155 d); the "~70-day realised" figure is the estimator-biased sample half-life on T = 800 paths (median 62–72 d across seed sets) and is ≈ 20 d (14–35 d across seed sets) on the 200-day windows the benchmark runs — review B, reproduced in `generated/v2_1/findings_reproduction.md`.]**
moments are kept as reference values for the checklist (single-stock |r| ACF(1) ~0.28, Hill ~2.8).

### The two additional scope questions
- **FW calibration data / compute:** resolved as in row 2 (attempt yfinance + SMM; documented fallback if infeasible).
- **L2b margin freeze:** the 10 pp margin is frozen at the end of E1 (first full-generator audit); any later change is an
  amendment to `PREREGISTRATION.md`.

Signed: as recommended, on the team's instruction of 22 Aug 2026 (recorded by the implementing agent).

## Addendum (E1, 22 Aug 2026): calibration choices made while building the generator, and open issues

All thresholds of Sections 5 and 9 are unchanged. The choices below are parameter values inside the plan's stated
anchors/options or implementation details the plan left open; the ones that depart from the plan's literal text are
also in `PREREGISTRATION_AMENDMENTS.md`. Variant runs: `generated/e1_calibration_variants.md`.

| Choice | Plan | Built | Why |
|---|---|---|---|
| FW calm half-life (fallback phi) | 60-120 d window | 120 d (phi = 0.579 at the realised fundamentalist share n_bar = 0.997) **[Phase 0 correction: stale — the shipped engine is `fw_fallback_hl150`, pull-rate half-life 150 d, φ = 0.4632, set after this addendum (row R1-D8)]** | sample ACF(1) of x must clear 0.98 (item 9); measured on T=800 paths (amendment A2) |
| FW weight normalisation | "normalised to unit mean" | divided by the pilot mean w_bar = 0.762 | equal-share normalisation (first attempt) gave innovations 0.5x too small because n_f ~ 1 |
| FW units | not stated | `price_scale = 100` (FW log price in percent) for the misalignment term | stated interpretation; to verify against the FW PDF; the re-estimation uses the same convention |
| GJR alpha / gamma / beta | 0.05 / 0.08 / 0.89 (anchor alpha 0.05-0.10, beta 0.85-0.93) | 0.10 / 0.10 / 0.83 (persistence 0.98 unchanged) | stronger clustering for items 3, 6, 8, 13; still the plan's persistence |
| t degrees of freedom | 5 | 5 (4 tried: helps item 2, hurts 3, 13, 20) | |
| sbar (calm x-innovation sd) | 1.6% | 1.7% | sample medians of calm sigma with t tails sit below the population value (item 20) |
| Rare jumps | optional, 0.004/day, N(-4%, 3%) | on, 0.008/day, N(-4%, 3%) **[Phase 0 correction: the shipped rate is 0.010/day (`GenConfig.jump_rate`); the 0.010 configuration was never run in the variant table `generated/e1_calibration_variants.md` (items 13, 72)]** | item 2 (kurtosis share) and item 8 |
| Panic variance multiplier | 4 (sensitivity 3-6) | 5 | item 13 (panic IV 60-100%) and item 20 (worst day) |
| Phase multipliers act on | omega | the whole conditional variance (regime-switching variance); omega-only kept as sensitivity | amendment A3 |
| Mania drift | compounding, uncapped | capped at g_max = 0.012/day (calibrated with the hazard) | amendment A5 |
| Hazard (h0, b) | "calibrated" | grid search with early-top (rejection) penalty, `tools/calibrate_hazard.py` | |
| Sustained bull | d_t = 0 | anchoring -0.15 x_t and variance x 0.25 | amendment A4 |
| Deterioration length | not stated | U(15, 40) d | design choice |
| Error-correction gains | not stated | panic 0.10, stabilisation 0.05, post-top 0.10 | make the scripted target paths binding |
| IV stress premium trigger | "0.35 in panic" | 0.35 when the GARCH variance is in its path's top decile (state-based) | a label-tied premium would be a phase clock (L2b) |
| Quarterly EPS | V / k | V / (4k) per quarter so that trailing-4Q P/E = k P/V | k is the annual multiple |

**Open issues after calibration (to be decided by the team; work on E3/E4 continues meanwhile because it does not
depend on them):**
1. Checklist 3 (volatility clustering): LB|r| p < 0.01 in ~55-60% of 200-day windows (criterion >= 80%) under every
   variant inside the plan's GARCH anchor with t(5) innovations; ACF|r|(1) 0.13-0.16 is inside the 0.1-0.4 band.
   Meeting the 80% share needs alpha >= 0.15-0.20 (outside the plan's 0.05-0.10 anchor) or thinner tails (which
   breaks item 2). Options: (a) accept the fail and report it; (b) amend the anchor (alpha up to 0.20, single-stock
   range); (c) amend the share criterion (e.g. >= 50% at p < 0.01 or >= 80% at p < 0.05).
2. Checklist 10 (delta matters): MDD means -57 / -48 / -41% at delta 0.55 / 0.70 / 0.85 (spread 16-18 pp; partial
   R2 0.32-0.38 vs the >= 20 pp and > 0.7 criteria). Delta now clearly matters (v1: 0.5 pp), but persistent
   calm-phase mispricing (item 9: sd 8-20%) and the plan's own panic variance add ~6-8 pp of drawdown noise that
   caps the partial R2. Options: (a) accept and report; (b) define the statistic on the event window (from event
   start) rather than the whole path; (c) relax to R2 > 0.5 / spread >= 15 pp.
3. Checklist 17 (rejection < 5%): bull_trap 7-11% (early hazard tops before x reaches 0.30 -- the joint conditioning
   the plan asks to publish), sustained_bull 11-20% (fat-tailed excursions outside [-0.10, +0.15] and V_T/V_1 < 1.2
   at the low end of mu ~ U(0.0015, 0.0025)). Rates and reasons are published per run.
4. Checklist 2 / 13 are at the margin (kurtosis > 1.5 in 71-79% vs 80%; corr(IV, RV) 0.38-0.40 vs 0.40) and will be
   settled by the 50-seed run.
5. **L2 surrogate (Section 5) -- structural.** v2 20-seed audit (`generated/leakage_audit_v2.md`): calm OOS R2(x) = 0.93,
   sign accuracy 0.98, event R2 0.97, MAPE(V) 4.1% (criteria <= 0.30 / <= 0.70 / < 0.90 / >= 10%). The PRICE-ONLY control
   already reaches calm R2 0.83 and MAPE(V) 3.8%: with sigma_V = 0.6%/day and a persistent mispricing that carries most of
   the price variance (plan blocks 1, 4), a long-window price average estimates V, so x is inferable from price dynamics
   alone. The valuation fields add +0.10 R2 (calm) / +0.03 (event) and ~1.5 pp of MAPE, i.e. their selectivity is inside
   the 10 pp L2b-style margin; the shuffled-V control is ~0 (no spurious fit). L1 passes (no formula beats price itself;
   amendment A6); L2b passes (selectivity +7.5 pp); L4 coverage at theta 0.05: flat 75%, crash 84%, bull 85%.
   Options: (a) keep the absolute thresholds and change the generator (raise sigma_V toward 1%/day and/or shorten x
   persistence), at the cost of items 9/20 and the plan's 'smooth fundamental' design; (b) re-state L2's pass criterion
   on SELECTIVITY of the non-price fields (full minus price-only R2 <= 0.10-0.15; MAPE gain <= 2-3 pp), consistent with
   C11's nuance that inference from price dynamics is legitimate, and report the absolute numbers; (c) accept the fail.
   The CI test is marked xfail(non-strict) with this note until the team decides; a selectivity test is added alongside.

## Decisions on the open issues (23 Aug 2026, made as recommended by the implementer; for team review)

Instruction: "make your recommended decision and document your justifications; I will review them later." Each
decision below is reversible: the alternative is a flag, a threshold constant or a one-line amendment. Thresholds of
the plan are changed only where stated (amendments A7, A8 in `PREREGISTRATION_AMENDMENTS.md`); everything else is
"report the pre-registered criterion as failed, with the reason".

| Issue | Decision | Justification | What changed |
|---|---|---|---|
| 1. Checklist 3 — LB\|r\| p<0.01 in 60% of 200-day windows (criterion 80%) | **Accept and report the fail; no threshold change; no parameter change.** Add `garch_strong` (alpha 0.15, gamma 0.10, beta 0.78) as an E6 sensitivity to show what the criterion would take. | Clustering IS present (ACF\|r\|(1) 0.15, inside the 0.1-0.4 band; ARCH-LM rejects 42%; refit persistence 0.96). Reaching an 80% rejection share on 200-day windows with t(5) innovations needs alpha >= 0.15-0.20, outside the plan's 0.05-0.10 anchor (Engle 2001: 0.077). Rewriting the criterion after seeing the data would be the post-hoc move pre-registration exists to prevent; the honest statement is "weaker-than-criterion clustering at the plan's anchors", with the sensitivity published. | none in code; sensitivity listed in CALIBRATION_REPORT §5 |
| 2. Checklist 10 — delta partial R2 0.35, spread 16.5 pp (criteria 0.7 / 20 pp) | **Amend the statistic to the EVENT-WINDOW MDD (A7), keep the thresholds, report the fail.** | Delta is the crash-severity parameter; whole-path MDD mixes in calm-phase mispricing excursions (item 9 requires sd(x) 8-20%, i.e. -20% calm drawdowns are by design). On the event window the result is partial R2 0.38 / spread 18.0 pp (means -53/-43/-35% at 0.55/0.70/0.85): the spread nearly meets 20 pp and delta is clearly causal (v1: 0.5 pp), but R2 > 0.7 is unattainable because the plan's own panic variance (x5; worst day -6..-15%) adds ~9 pp of drawdown noise per path. Relaxing R2 to ~0.4 would be tuning to pass; reported instead. | `evaluation/stylized_facts.py::item10_delta_matters` (both statistics reported; gate on event-window) |
| 3. L2 surrogate — absolute thresholds fail because price dynamics alone predict x (calm R2 0.83 price-only) and V (MAPE 4%) | **Re-state the L2 pass criterion on SELECTIVITY of the non-price fields (A8): best-full minus best-price-only R2(x) <= 0.20 in every phase group, MAPE(V) gain <= 5 pp, shuffled-V R2 < 0.10 (margins frozen with headroom above the observed 0.13 / 2.9 pp, as the plan does for L2b); report the absolute numbers beside it.** | The leakage the plan guards against is leakage THROUGH FIELDS (the v1 P/E inversion); inference of mispricing from price history is what a fundamentalist does and the plan itself (C11 nuance, L2b) treats price-based phase inference as legitimate and tests only the selectivity of non-price fields. With sigma_V = 0.6%/day and a persistent x carrying most of the price variance (blocks 1 and 4, both plan-specified), x is inferable from a long price average in ANY generator that meets items 9 and 20, so the absolute thresholds are inconsistent with the plan's own design, not with the implementation. Result on 20 seeds: max non-price R2 gain 0.13, MAPE gain 2.9 pp, shuffled-V -0.004 -> pass; absolute: calm R2 0.93 / event 0.97 / MAPE 4.1% -> fail, published. Alternative (raise sigma_V toward 1%/day, shorten x persistence) would break items 9 and 20 and the plan's 'smooth fundamental' rationale (Campbell et al. 2001). | `evaluation/leakage_audit.py::l2_verdict(mode)`, constants `L2_SELECTIVITY_R2/MAPE`, `L2_SHUFFLED_MAX`; checklist row 14; CI test gates on selectivity and documents the absolute fail |
| 4a. Checklist 17 — sustained-bull rejection 24%, bull 5.7% | **Accept; publish rates and reasons per run; no change.** | Rejection is transparent conditioning (plan C9): the published rate is the information. Sustained-bull rejections come from fat-tailed/jump excursions outside the plan's narrow validity band and from V_T/V_1 < 1.2 at the low end of the plan's mu range; removing them would mean regime-specific jump/tail settings (a label-tied difference) or widening a plan-stated band. Bull 5.7% is the early-hazard-top conditioning the plan asks to publish. | none |
| 4b. FW units (`price_scale = 100`) | **Keep the stated interpretation; flag for verification against the FW 2012 PDF before the index set is cited as a sensitivity.** | The linear pull (the only term that matters for the fallback) is unit-free; the SMM re-estimate was rejected (J = 408, not identified), so no main result depends on the interpretation; only the `fw_index` sensitivity does. | none; note in E1 spec §2 |
| 5. Margin items 6 (68% vs 70%), 7 (49% vs 50%), 13 (IV 59.4 vs 60; corr 0.39 vs 0.40) | **Accept as-is; report with the 50-seed statistics; do not re-tune.** | Each is within one sampling sd of its threshold on 50 seeds; parameter nudges flip them at random (variant table) and every nudge costs a full re-validation. A later 200-seed run will settle them; E6 reports them as marginal. | none |

Net effect on the published table (50 seeds, after A7): checklist 9 pass / 6 fail / 7 n-a unchanged in count (item 10
still fails on the event window); Section 5: L1 pass (A6), L2 pass on selectivity (A8) with the absolute numbers
reported as failing, L2b pass, L4 published. The pre-registration amendments file records A7 and A8 with these
justifications; the team can revert either by changing one constant / one function and re-running the two audits.

## Decisions to unblock the remaining items (23 Aug 2026; team instruction: "run your recommended way and document")

| Item | Decision | Justification |
|---|---|---|
| E5 stateful arm — context design | **Rolling-window transcript (last 20 steps as prior human/assistant turns, ~12-18k tokens) as the primary stateful arm; a `full` mode (entire transcript up to a 60k-token budget, oldest dropped) as the second arm; summarisation memory deferred.** Mandate in the SYSTEM prompt at t = 0 for every stateful arm (Path B requirement 2); `stateful_memory` re-injects per step. Per-step logging of `Context_Tokens` and `Mandate_Offset_Tokens` (distance from the end of the mandate span to the generation point) and `Context_Turns`. | The plan (11.1) requires the context design to be chosen explicitly because each makes "context accumulates" mean something different. Rolling window is the cheapest design that makes the mandate's distance grow (then plateau), is directly comparable with the forced-closure literature (When Attention Closes), keeps per-call cost bounded (~15k tokens), and keeps the stateless grid as the control; `full` shows the unbounded-distance regime. Summarisation introduces a second design axis (the summariser) and is left as a later arm. Implemented in `agent/stateful_agent.py`; arms `stateful_static`, `stateful_memory`, `stateful_full_*`. |
| Multi-asset extension scope | **Runnable now at N = 3 (`--three_asset`): scenario asset + correlated peer + defensive low-vol risky asset (vol x 0.5, common factor 0.3); event applies to all; per-asset hazard; agent states cash share + sleeve weights; per-asset x/V/weights logged. Scoring in the extension: the cash-share band on total cash (plan 4.4); per-asset resolvability reported; MCR on the scenario asset; a weights oracle is NOT implemented (documented).** | Decision 8 put the 3-asset scenario outside the main grid; building the runnable configuration now means the extension is a configuration, not a rewrite, as the plan asks, while the per-asset evaluation layer (weights oracle, per-asset audits) is deferred until the N = 1 results exist. |
| Statistics track (11.2) | **`tools/stats_v2.py`: model-level mixed-effects (metric ~ arm x persona with random intercepts for model and seed; statsmodels MixedLM), Benjamini-Hochberg across the metric family, Cliff's delta and Hedges g with bootstrap CIs for each arm contrast within persona, and the degeneracy audit table; tested on fake-LLM runs.** | The plan names this track as one that "falls between the two plans" unless owned; writing it against the v2 run schema now means it runs the day E7 data exist. |
| FW single-stock estimate | **Keep the documented fallback; do not add ad-hoc identifying moments.** | The nine FW return-moments do not identify the mispricing parameters (J = 408); adding fundamentals-based moments needs a per-stock fundamental proxy that the plan has not specified, which would be a new modelling choice made to reach an estimate. The fallback is transparent; the index set is the sensitivity. |
| Version control | **All code stays on `main` (team instruction, 23 Aug 2026: no separate branches); commit on `main`, no push, no force.** | The team keeps a single line of development; the commit gives a stable reference for the pilot and the review. |
| Pilot size | **Smoke test first (4 runs, T = 30, Gemini 2.5 Flash), then a very small pilot: Gemini 2.5 Flash only, 3 personas x 4 arms (static, memory, placebo_directive, swapped) x 3 scenarios (flat, bull_trap, crash 0.70) x 1 seed, T = 200 = 36 runs (~7,200 calls, ~1 h, a few dollars), plus the common-start static slice for the gate (9 runs) and one stateful_memory cell per persona on flat (3 runs) to exercise E5.** | Validates every harness component and the report on real model output at trivial cost; no headline claims are made from it. Larger runs wait for the team's review of the decisions above. |

## Review round (23 Aug 2026): two independent reviewer agents and the resulting changes

Two reviewers were run on the decisions above (R1: forensic / pre-registration-integrity lens; R2: quantitative
methods lens). Their verdicts are summarised with the action taken; where I disagreed, the reason is stated.

| Reviewer point | Action |
|---|---|
| R1-D3: amendment A8 (L2 gate on selectivity with margins chosen after seeing 0.13 / 2.9 pp) is tuning-to-pass; row 14 must not be PASS; the absolute fail and its meaning ("hidden value" is hidden from fields and algebra, not from price dynamics) must be stated | **Accepted.** L2 gate reverted to the pre-registered absolute thresholds (FAIL, reported); selectivity of the non-price fields reported as EXPLORATORY (best-vs-best 0.10 R2 / 2.2 pp; worst-model 0.13 / 2.9 pp -- both definitions stated); checklist row 14 = FAIL; CI test is a strict xfail with the explanation; the audit text states that price-only inference of x is possible and that the rule-based baselines quantify what a price-only policy captures. A8 recorded as WITHDRAWN as a gate; the amendments header corrected. |
| R1-D5: sustained-bull variance x0.25 makes the control identifiable from volatility alone (a scenario clock through IV) and confounds the bull-trap vs sustained-bull comparison | **Accepted.** Multiplier reverted to 1.0 (anchoring drift kept); the published rejection rate rises (~37% on 30 seeds; checklist 17 reports it); a scenario-discrimination audit (price-only / price+IV / full classifier: sustained-bull vs mania vs calm days) added to the audit report. |
| R1-D7/D9: items 11 and 9 are calibration targets, not independent validations | **Accepted** -- labelled as such in the spec and calibration report. |
| R1-D8: reconcile half-life numbers (spec 120 d vs log 150 d vs realised 72 d) and the jump rate (0.008 vs 0.010); SMM non-identification asserted, not shown | **Accepted.** Spec corrected (150 d stationary design value, 72 d realised, jump rate 0.010); `tools/calibrate_fw.py --profile` publishes the J-profile over phi (`generated/fw_J_profile.csv`); the signed "re-estimated on single stocks" deliverable is stated plainly as NOT achieved. **[Phase 0 correction: "72 d realised" is the estimator-biased T = 800 sample value (review B); the pull-rate 150 d, the 147-d long-pilot value and the ≈ 20-d 200-day-window value are now stated together in every document]** |
| R1: L2 audit at 20 seeds < plan's 50 | **Accepted** -- 50-seed audit run. |
| R1-D2: A7 is a post-hoc statistic change | **Accepted as a disclosure**: A7 kept (event-window MDD is the right construct for a severity parameter), stated as adopted after seeing data, thresholds unchanged, fails under both definitions. |
| R1-D1 / D4 / D6 disclosures (clustering and kurtosis at the low end of single-stock values; control rejection selects a smoother subsample; regime-switching variance is itself a phase channel measured by L2b) | **Accepted** -- added to the spec and calibration report. |
| R2-1 (L5): regress x, not classify c*; full and price-only variants; per-phase OOS R2; disjoint training seeds; MCR before return | **Accepted** -- `evaluation/observables_oracle.py`, `tools/l5_report.py`. |
| R2-2 (multi-asset): existence-based cash rule; total-variation weights regret masked by sleeve size; relative resolvability on the spread; audit the full N-asset vector with the own block as control; do not name the defensive asset | **Accepted** -- `metrics_v2.multi_asset_oracle/_regrets`, `synthetic_market.audit_panel_multi`, wording level `defensive` ("favour lower-volatility holdings"; a level, not the headline). |
| R2-3 (summary arm): not comparable to rolling-20; summariser is a re-injection channel | **Accepted** -- neutral note-taker summariser prompt, summary text + mandate-mention flag logged, matched rolling-5 control arms (`stateful_r5_*`); k = 10 / m = 5 kept and disclosed. |
| R2-4 (time vs phase): per-day series near-unit-root; permutation null breaks autocorrelation | **Accepted** -- mixed model on 25-day window means with a random intercept per run and an ordering factor; circular-shift null for phase-free runs; paired stateful-minus-stateless sign-flip for "time beyond phase". |
| R2-5 (FW): keep the fallback; publish the J-profile; bracket the half-life with 60 d and index sensitivities | **Accepted** -- engines `fw_hl<days>`; `checklist_v2_sens_hl60.md`; index/pruna sensitivities. |
| R2 suggestion to add fundamentals-based SMM moments | **Declined** (a new modelling choice made to reach an estimate; R2's own caveat). |

**L5 result (23 Aug 2026):** the observables oracle nearly matches the V-oracle (MCR gap 0.02-0.08; x_hat OOS R2 0.90-0.96;
price-only within 0.01 of the full set). Decision: report as the headline "withheld information" number with the
disclosure that, under the plan's smooth-V / persistent-x design, mispricing direction is inferable from observables
(consistent with the L2 absolute failure); the environment's claim is leak-free FIELDS and algebra, not an
uninferable mispricing. No change to the generator (changing it would break items 9/20 and the plan's own rationale).

## Phase 0 of the v2.1 programme (29 Aug 2026): verification and freeze — decisions and corrections

Governing documents: `docs/env_v2/v2_1/V2_1_IMPROVEMENT_PLAN.md` Section 4, `docs/env_v2/v2_1/PREREG_PHASE_0.md` (written
before any run), `docs/env_v2/v2_1/PHASE_0_REPORT.md` (results). Phase 0 contains no design decision; every entry below is a
correction of an implementation to its own documentation, a removed silent override, or a labelling decision. Every path
column except the analyst field is bit-identical before and after (`generated/v2_1/path_hashes_{before,after}.json`).

| # | Decision | Chosen | Label | Reasoning and evidence |
|---|---|---|---|---|
| P0-1 | Analyst error interpretation (weakness 68) | **The documented process is authoritative**: u is an AR(1) with ρ = 0.95 per weekly update and stationary sd 0.15; the √5 innovation scaling (`observables.py::analyst_block`) that made the implemented sd 0.15 √5 = 0.335 is removed. | DESIGN (the 0.15 itself; Phase 5 decides its value from the read anchor, a 45 % absolute target-price error, LOG §4.3) | Reproduced before the fix: pooled sd(u) 0.352 [0.323, 0.377] on benchmark days (100 seeds), median \|F/V − 1\| 0.245 (the deck's "22 %"); after the fix: see the Phase 0 report (test `test_analyst_error_sd`, ± 10 % of 0.15 at 100 seeds). The alternative — documenting 0.335 — would have kept an undocumented parameter that no source supports either; making the code match its documentation defers the value question to the phase that owns it. |
| P0-2 | MCR normalisation labels (weakness 53) | **Labels only**: `floors_and_ceilings` normalises every lower-is-better metric, MCR included, against constant-mix (ceiling) and the worst of {always-buy, always-sell, random} (floor); `report_v2.py`, `PILOT_NOTES.md` and the docstring now say so; the published pilot `norm_mcr` values are annotated "normalised against constant-mix". | — (amendment A9) | No number changes. The pre-registration's RG-type rule (ceiling = mandate oracle) and the implemented rule disagree; which convention v2.1 adopts, with the regret decomposition, is Phase 7 (E7.2, REG-12). |
| P0-3 | Day-1 gate input (weakness 54) | The gate is computed on common-start cells only; the start-at-target ΔC_1 table is renamed `exploratory_deltaC1_start_at_target` and carries the reason. | — (amendment A9) | A level/band-membership gate fed a difference cannot pass under start-at-target (pilot band-hit 0.0). Re-specification is Phase 7 (E7.5). The pilot report is regenerated with the new labels; per-run metrics unchanged. |
| P0-4 | Silent hazard override (weakness 69) | `HAZARD_H0 = 3e-4`, `HAZARD_B = 6.0`, `G_MAX = 0.012` in the modules equal the calibrated values; `load_hazard_params()` raises at import if `params/hazard.json` is missing or disagrees. | CAL (unchanged values) | Before: module constants 0.0015 / 6.0 / 0.02 were silently overridden by the file; a missing file would have run different parameters without warning. No path changes (the generator always used the file's values). |
| P0-5 | Engine naming (weakness 70) | The engine that runs is named `fw_fallback_hl150` (`ENGINE_DEFAULT`); `engine="fw_single"` raises unless an accepted estimate (`"accepted": true`) exists; `get_metadata()` records `engine_used`. | CAL (unchanged parameters: φ = 0.4632, price_scale 100) | Before: `fw_single` fell back silently and renaming `fw_single_stock.REJECTED.json` would have switched the engine to a rejected estimate. Consequence: `Gen_Config_Hash` of every future run changes (the engine string changed); no path changes. |
| P0-6 | Half-life statements (weakness 71) | Every document states: pull-rate half-life 150.0 d (CAL); long-pilot ACF(1) half-life 147 d (five 200,000-step pilots, 141–155 d); sample half-life ≈ 20 d on 200-day windows (14–35 d across seed sets) and 62–72 d on T = 800 paths (estimator-biased, review B); the 188-d figure is withdrawn. | — | The 188 d came from one 20,000-step pilot whose ACF(1) has a sampling SE of ≈ 0.0007 (LOG §1); `test_half_life_consistency` (hard, ± 8 d) locks the agreement at 200,000 steps: 146.6 d vs 150.0 d. |
| P0-7 | Stationary sd(x) — the 0.13–0.14 vs 0.165 discrepancy | **Both numbers are right and measure different things**: `pilot_stats` runs with raw weights (≈ 0.76) and prints sd(x) 0.126–0.134 (0.142 on its 20,000 steps); the engine runs with unit-mean weights and has sd(x) 0.165 (sd_e 0.016) / 0.175 (sd_e 0.017 = the calm sbar); ratio 0.762 = w̄ 0.761 (hypothesis pre-registered in PREREG §8, confirmed). Documents state the engine's value with this explanation. | — | Five 200,000-step pilots per setting (`findings_reproduction.md`, block R13). Consequence for Phase 1: the Kalman bound of the plan's Appendix B should be read at s_x ≈ 0.17–0.18 (its 0.165 row), not 0.13. |
| P0-8 | 200,000-step re-normalisation of w̄ / n̄ | **Deferred to Phase 2.** The plan's "re-run pilot_stats at 200,000 steps" is applied to the reported statistics (`long_pilot_stats`), not to the normalisation constants. | — | A 200,000-step normalisation would move n̄ 0.99765 → 0.99734, w̄ 0.76113 → 0.76154, φ 0.46319 → 0.46333 (+0.03 %) — and therefore every path (PREREG §7 forbids that in Phase 0). Phase 2 re-derives φ anyway. |
| P0-9 | Item 73 small items | Runner trader band (0.4, 0.6) → (0, 1) as in the metrics; the common factor's t-mixture documented as not t(5); crash μ_V = 0 after deterioration documented as a stated choice pending Phase 4 (E4.8); the $1 holdings threshold named `V1_RULE_HOLDINGS_THRESHOLD` and documented. | DESIGN (documented) | The trader band affects only the logged `Band_Lo/Hi` columns of trader runs (band-MAS for the trader is band-free in the metrics anyway); no scoring changes. |
| P0-10 | Sensitivity footers (weakness 39) | All seven checklist `.md` files regenerated from their CSVs (`tools/regen_checklist_md.py`); locked by `test_footer_counts_match_csv`. | — | Correct counts: default 8/7, fw_index 8/7, pruna 7/8, hl60 7/8, omega 7/8, panic×3 9/6, panic×6 8/7 (of 15 applicable). |
| P0-11 | Freeze (weakness 65) | `tests/v2_freeze_manifest.json` (SHA-256 of `envs/v2/**/*.py`, the facade, `envs/v2/params/*.json`, the six evaluation modules; CRLF-normalised); `Env_Code_Hash` for a v2 environment is the manifest hash. **Regeneration rule**: the manifest is rewritten only by `python -m tools.freeze_manifest --write` at a phase end, and the regeneration is logged here. Written once at the end of Phase 0 (label "Phase 0 freeze"). | — | Execution-order step-0 rule adopted (plan 0.6): "anything that changes the environment or the prompts after step 0 restarts from step 0: re-freeze, re-audit, new hashes" (execution-order document, paragraph 59); the main grid runs only on the freeze produced at the end of Phase 10; every phase end re-runs the checklist and audits on the frozen state it hands over. The smoke gate (100 % parsed, all rows present, provenance columns filled, rendered prompt equal to the published sample) is the Phase 8/9 run gate. |
| P0-12 | Legacy tests (weakness 66) | `tests/test_market_environment.py` skips without `vectorbt`; `tests/test_env_logic.py` rewritten against the v2 contract. **Finding**: the legacy plateau assertion (\|V_1 − V_50\| < 1, P_50 > 140) never held on the frozen v1 either (V 100.60 → 96.32, P_50 = 125.93 at its own seed): the test was written in March 2026 against an earlier draft generator. The v1 facts are asserted positively in `tests/test_provenance_and_freeze.py::test_v1_bull_trap_value_plateau_frozen`. | — | The plan's "which was true of v1's plateau" (§0.3) is corrected. |
| P0-13 | Known-defect registry (weakness 66) | `tests/known_defects.py` lists every strict-xfail test of the v2 generator with items and owning phase (defects 4/13 → Phase 1; 2/11 → Phase 2; 46/25 → Phase 3; 18/42 → Phase 4; the L2 gate 5/32 → Phase 6); printed at the end of every pytest session; a meta-test keeps it equal to the strict xfails in the suite. v2.1 acceptance: the v2 section is empty. | — | Strict xfails keep the suite green while a defect persists and fail (XPASS) the moment it disappears, so no fix goes unnoticed and no defect is forgotten. |
| P0-14 | The L1 rule (amendment A6) after the analyst fix | **Registered as a known defect, not moved**: with the analyst field at its documented sd 0.15 the k·analyst candidate lies inside the 1 % floor on about as many steps as price itself (50-seed audit: 95.2 % of steps above the floor vs price 95.9 % — PASS by 0.26 pp under A6's "+1 pp" clause; 8-seed CI panel: 93.6 % vs 96.4 % — FAIL). `tests/test_leakage_ci.py::test_v2_L1_no_algebraic_inversion` is a strict xfail with a registry entry (items 5, 21, 32), owner Phase 6 (E6.5 derives the L1 pass rule from the x noise floor) after Phase 5 decides the analyst field. | — | Moving the floor or the +1 pp clause after seeing the data would be tuning-to-pass; the pre-registered rule is kept and its marginality is published. The corrected field is a closer estimate of V, so the L1 candidate set (item 5) now shows what the reviews said it would once the √5 defect was gone. |

## Phase 1 of the v2.1 programme (29–30 Aug 2026): value and price structure — decisions

Governing documents: `docs/env_v2/v2_1/PREREG_PHASE_1.md` (written before any run), `PREREG_PHASE_1_ADDENDUM.md` (criteria
found wrong and corrected in documented steps), `PHASE_1_REPORT.md`. Team decisions applied: D1 = C (hybrid), D13 =
provisional B, D16 = full programme. Nothing committed; every entry names its evidence file under `generated/v2_1/`.

| # | Topic | Decision | Alternative | Rationale / evidence |
|---|---|---|---|---|
| P1-1 | Panel exclusion rule and analysis sets (E1.0 report Q7) | Every series with `reuse flag > 0` excluded (86 of 673), two '.'-spelled duplicates dropped, window 2000-01-03..2024-12-31, Adj Close returns; set A = 417 flag-free full-history names (primary), set B = 155 shorter flag-free series. DESIGN, set from the manifests before any statistic. | Keep the flagged series with a per-series start date | A flagged series is a different company for part of its history (E1.0 §1.2–1.3); the rule is the one the manifests can apply mechanically. `e1_0_analysis_sets.{csv,json}` |
| P1-2 | One standard evaluation panel for every before/after row | SEP = `audit_panel` composition at seeds 30000–30199 (event-first 31000–31199), 1,600 paths, 320,000 rows, T = 200; no subsampling; the before-panel generated from the frozen code and stored (`_panels/sep_before.pkl`). | Re-use the published 50-seed audit panel | Like-for-like rows need identical seeds, rows and estimators; the frozen paths are stored so the comparison survives every later code change. |
| P1-3 | Jump placement (E1.4; REG-3) | `jump_placement = "x_zero"` (mean-zero jumps in x) by the pre-registered KS rule (all four placements pass, smallest window-day distance). E[x] on 1,000 flat paths +0.0126 [+0.0038, +0.0214]: fails the ±0.02 TOST by its upper limit; with jumps off entirely +0.0113 [+0.0027, +0.0199] → the residual is the engine's, registered as a known defect owned by Phase 2. v2's negative-mean placement (E[x] −0.069) kept behind the switch only. | A (jumps in V at announcement days) or C (both): KS-equivalent, q 0.27–0.28 vs the panel's 0.43 | `e1_4/generator_split.md`, `confirm_B_x_zero.json`, `confirm_jumps_off.json`, `panel_split.md` |
| P1-4 | Burn-in (E1.5; REG-17) | Default engine: A = 750 d (≥ 5 half-lives); fw_hl60, fw_index, pruna, ar1: B = stored day-5,000 state + 60-day warm-up (`params/burn_in_states_<engine>.npz`). `GenConfig` resolves days and mode per engine from `value.json`. At 2,000 paths: v2's 260 d passes for the 150/60-day engines and fails for the 620-day ones (KS_x 0.17). | Keep 260 d everywhere | `e1_5/burn_in.md` (2,000 paths), `burn_in_n500.md` (the pre-registered 500-path run, undecidable) |
| P1-5 | Two pre-registered equivalence criteria found undecidable | E1.4's ±0.02 TOST at 200 seeds (half-width 0.020) and E1.5's KS bound at 500 vs 500 paths (upper limit ≈ 0.12 under equality) are reported as written (fail) and corrected in `PREREG_PHASE_1_ADDENDUM.md` §2 and §4 (1,000 seeds; 2,000 paths), each derived before the powered run with disclosure of what had been seen. **For ratification by the team.** | Move the margins | Protocol §1: a wrong criterion is stated, the corrected one derived in a separate documented step, results under both; the margins and statistics are unchanged, only the sample sizes. |
| P1-6 | μ_V, df_V, start-price range | μ_V = 0.000228/day FIT (Shiller price-only 2000–2024; v2 0.00025 stipulated). df_V: DESIGN, t₅ in force and Gaussian carried through E1.3 — the panel's EPS-surprise tails are equivalent to neither (KS upper 0.20 / 0.18). Start-price range for mechanisms A/C: P5–P95 of set-A unadjusted Close on 40 random dates, 7.47–240.02 (FIT). | — | `e1_2/misc_fits.md` |
| P1-7 | The L5 before-row | Generated under `FROZEN_CFG` (`tools/phase1/before_state.py`), verified to reproduce every hidden column of the Phase-1 before-hashes on all 95 manifest configurations; the six EPS/dividend rendered fields differ only through the announcement-lag draw (new RNG stream). Stated as a deviation. | Re-check out the frozen code | `e1_6/frozen_equivalent_check.json` |
| P1-8 | Recovery-study scope for estimator C | 8 replications in the 6 cells nearest the estimators' data values (h ∈ {30, 150, 250} × σ_V ∈ {0.006, 0.012}), data-side interval from 10 refits; A and B at the pre-registered 200 in all 12 cells. Fixed before any C cell ran; one C fit costs 310 s here. | The pre-registered 600 fits (≈ 17 h) | `PREREG_PHASE_1_ADDENDUM.md` §5 |
| P1-9 | σ_V, h, s_x (E1.2; REG-5 / REG-2 / D3) | **None adopted → D3.** By the pre-registered usability rule no estimator is usable on the recovery panels: A is misspecified for the engine's mispricing (recovers h on an exact AR(1) panel, overstates it 1.3–4 × on the engine's), B measures the V̂ error's within-quarter structure (ĥ ≈ 38 d even with x ≡ 0), C is *undecided* at 8 replications (median error 0.16 at its nearest cell against the 0.20 boundary, ADDENDUM §5). v2's σ_V = 0.006 stays in force, labelled "D3 PENDING"; no h / s_x target. Evidence to the team: A and C agree on σ_V ≈ 0.020 [0.019, 0.021] (3.3 × the value in force) and on h ≈ 5 d. | Adopt 0.020 from the agreeing estimators; or wait for Phase 2's engine re-fit (E2.3/E2.6); or the WRDS re-fit (D1) | `e1_2/recovery.md`, `decision.md`, `smm_fit.json`, `recovery_diagnostics.json`; consequences in `e1_3/sweep.md` |
| P1-10 | Start-price mechanism (E1.1; D13) | **B stays in force, D13 re-opened.** B (P₁ ≡ 100) fails the attacker rule (Δ +0.08 / +0.21 / +0.12, intervals above 0: the level encodes the return since day 1) and the rule-100 test (its MCR beats both band edges in every scenario); A and C pass both. `test_start_price_carries_no_information` is a registered strict xfail (items 1, 3, 43; Phase 9 / D13). | Switch to A or C now | `e1_1/start_price.md` |
| P1-11 | L2b phase-clock gate under the level-free control | Registered (Phase 6): selectivity 11–16 pp against the 10 pp margin once the control no longer carries the level; `test_v2_L2b_phase_clock_selectivity` strict xfail. L1's registered marginal failure (P0-14) disappeared under the Phase-1 state (passes at the CI seeds) and its entry is removed (XPASS rule); the rule stays marginal and is re-derived in Phase 6. | Keep the v2 level control for the CI test | `e1_6/audit_before_levelfree.md`, `tests/known_defects.py` |
| P1-12 | Freeze manifest | `tests/v2_freeze_manifest.json` rewritten on the handed-over state (label "v2.1 Phase 1 freeze"; 19 files; `value.json`, `value_params.py` added); `path_hashes_phase1_after.json` written. Execution-order rule honoured: the SEP audits, L5, checklist and hashes were produced on this state. | — | `tools/freeze_manifest.py`, `tests/test_v2_freeze.py` |
| P1-13 | D13 revised (30 Aug 2026, team, after reading the Phase-1 report) | **Mechanism C (`both`) replaces the provisional B** for Phases 2–6: `start_price_mode = "both"` (P₁ ≡ 100 internally, a per-seed render scale k ~ LogU(7.47–240.02 / 100) on every price-denominated rendered field). C passes the attacker rule (Δ −0.03 / −0.06 / −0.03, intervals below 0) and the rule-100 test, keeps the hidden paths identical to B's, and defeats the cross-path `k·analyst_fair_value` inversion (median APE 0.10 → 0.70). After-state regenerated under C; B's outputs kept as `*_B`. Registry entry for the E1.1 test removed (the test is hard again). | A (random hidden level): also passes both rules; changes the hidden paths and re-opens every Phase-1 audit; kept for REG-1's Phase-9 LLM test | `e1_1/start_price.md`, `PREREG_PHASE_1_ADDENDUM.md` §6.3, `tests/test_v2_1_phase_1.py::test_render_scale_invariance` |
| P1-14 | Post-review extensions (team instruction: "we need robust results; the plan may change on the results") | (a) Recovery study at the pre-registered 50 replications for estimator C in every cell **plus h ∈ {5, 10} d** for A, B, C (Kaggle kernels `fp-p1-rec-*`; the data-side SMM at 30 refits); the decision rules of PREREG §4.5 re-applied and P1-9 amended on the result. (b) Calm-reference reconciliation between E1.3's surrogate and the SEP audit (`tools/phase1/e1_3_calm_reconcile.py`). Designs fixed in ADDENDUM §6 before the runs. | — | `PREREG_PHASE_1_ADDENDUM.md` §6 |
| P1-15 | D3 answered: σ_V, h, s_x (E1.2, full-scale recovery study, 30 Aug 2026) | **Estimator C adopted** — the only usable estimator once every cell ran at the pre-registered replication counts and the grid was extended to h ∈ {5, 10} where the data sits (C: median relative error 0.03–0.16 for h ≤ 150, 0.05 at its own cell; A: never usable, interval coverage 0.00–0.01; B: returns ĥ ≈ 36 d whatever the truth, and the same on a panel with x ≡ 0). Adopted values: σ_V 0.01957 [0.01892, 0.02036], h 4.82 d [4.17, 5.42], s_x 0.129 [0.125, 0.135] **[Phase 2 correction, 1 Sep 2026: the s_x figure is the engine's sd(x) at the reference 150-day half-life, not at the fitted 4.82 d; re-measured on the fitted process it is 0.0250 — P2-6]**. `h_fit` and `s_x_fit` written to `value.json` (Phase-2 targets). **σ_V recorded as adopted-not-yet-applied**: it is a generator parameter, so applying it triggers the execution-order rule (E1.4/E1.5/E1.1 and the whole after-state were measured at 0.006). Team choice in PHASE_1_REPORT.md §6.1: (a) apply now and re-run the cascade, (b) fold into Phase 2's engine re-fit (recommended). Qualifications recorded with the adoption: the recovery panels come from C's own model, so the test favours C by construction; J = 66.6 shows the model does not fit set A's moments, and the pre-registration sets no J threshold at this stage. | Keep v2's stipulated 0.006 (no estimator adopted) — the outcome of the 8-replication run, reversed by the full-scale study | `e1_2/recovery.md` (32 cells), `decision.md`, `smm_fit.json`, `recovery_diagnostics.json`; `PREREG_PHASE_1_ADDENDUM.md` §6.1 |

## Phase 2 of the v2.1 programme (1 September 2026): the mispricing engine and its persistence — decisions

Governing documents: `docs/env_v2/v2_1/PREREG_PHASE_2.md` (written before any run), `PREREG_PHASE_2_ADDENDUM.md`
(designs found unmeetable or compute-bound, corrected in documented steps before the runs they govern),
`PHASE_2_REPORT.md`. Team decisions applied: D1 = C (hybrid), **D3 = (b)** (the execution prompt's blank is
empty, so σ_V is a free parameter of E2.3 and is applied once, from the fit), D13 = mechanism C, D16 = full
programme. Every result in this section was produced on the local reference machine; nothing was offloaded.
Nothing committed; every entry names its evidence file under `generated/v2_1/`.

| # | Topic | Decision | Alternative | Rationale / evidence |
|---|---|---|---|---|
| P2-1 | The plan's SABCEMM target for DCA-HPM (LIT correction) | **The plan's "average chartist share 0.23, excess kurtosis 7.8" is wrong and its "[changed: LOG §4.2]" note inverted a reading that was right in the first draft.** SABCEMM's Table 1 (arXiv:1812.02726, read at source 1 Sep 2026, 200 runs × 7,000 steps) gives DCA-HPM **10.033 / 2.481 / 0.1674** (excess kurtosis / Hill / chartist share); the "0.17 / 10" pair *is* the DCA-HPM row. The plan's pair is closest to the **DCA-WP** row (0.2285 / 7.7600). E2.1 compares against the corrected values; the plan itself is not edited (the execution prompt forbids it). | Keep the plan's pair as the target — it would have made E2.1's secondary arm a test against a number no row of the source contains | `e2_1/fw_repro.md`; SABCEMM Table 1 and Tables 6–12 |
| P2-2 | The Franke–Westerhoff units convention (E2.1; the plan's expected bug fix) | **`price_scale = 1` is confirmed and `price_scale = 100` is refuted.** FW's own model at their DCA-HPM parameters gives a joint moment coverage ratio of **10.0 % [6.6, 14.9]** against their published Table 4 value of **10.1 %** at scale 1, and **0.0 % [0.0, 1.9]** at scale 100, where the switching term saturates (chartist share 0.0028, ACF of \|r\| 0.000 at every lag). LIT. The fix is applied to the engine that RUNS and to `tools/phase2/fw_pure.py`; the **legacy generator engines (`fw_fallback_hl*`, `fw_index`, `pruna`, `ar1`, `fw_hl*`) keep `price_scale = 100` deliberately**, behind their own names, so that every v2 number and every Phase-1 stored artefact (`params/burn_in_states_*.npz`, the E1.5 day-5,000 states) stays reproducible — the same "every change is a switch with the v2 behaviour behind it" convention Phase 1 used. | Change `price_scale` globally: it would silently invalidate Phase 1's stored burn-in states for four engines and every v2 comparison, for no gain in the engine that runs | `e2_1/fw_repro.{json,md}`; FW 2012 eqs (1), (5)–(7), Tables 1, 4, A1 read at source |
| P2-3 | The SABCEMM DCA-HPM row is not reproducible | Reported as an **unexplained discrepancy with a published source**, not resolved: FW's published equations at their published parameters give a chartist share of 0.2987 [0.2917, 0.3057] and an excess kurtosis of 1.781 [1.744, 1.817] at 200 runs — 11 and 220 half-widths from SABCEMM's row — under either units convention. The one ambiguity in SABCEMM's own appendix (their eqs (5)–(6): `EDF = ½(ed_f + ed_c)` with `ed_f = 2 n_f d_f`, which doubles the price impact under one reading) was run as a **pre-registered diagnostic** and moves the share to 0.219 but not the kurtosis (1.75), so it does not explain the gap. **No parameter of any engine is set from this arm**, and E2.1's verdict rests on FW's own Table 4 statistic instead. | Tune μ (or any other parameter) until SABCEMM's row is matched — that is fitting to a summary statistic of a third-party re-implementation, which the plan's rules forbid | `e2_1/fw_repro.md` (diagnostic row) |
| P2-4 | The simulation-noise rule of PREREG §4.3 | **Re-scoped, before any fit, in `PREREG_PHASE_2_ADDENDUM.md` §1**: the rule as written ("every evaluation ≤ 0.30 data-bootstrap sd", ladder 20 → 50 → 100 paths) has an empty acceptance set — the measured ratios are 1.4–2.3 at 20 paths and 0.68–0.93 at 100, and 0.30 per evaluation would need ≈ 960. The optimiser runs at 200 paths under common random numbers; the **reported** moment vector and J at θ̂ average K = 20 CRN replicates, giving ≈ 0.15 bootstrap sd — inside the bound. FW's bootstrap p-value, which simulates at the data's own panel size, is unaffected and is the primary acceptance criterion. Both J values are reported. | Raise the path count to ≈ 960 per evaluation: a tenfold cost on the phase's most expensive stage for a precision that only the reported J needs | `e2_0/power.json` (PA2); `PREREG_PHASE_2_ADDENDUM.md` §1 |
| P2-5 | Compute-bound reductions | Fixed **before** the runs they govern (`PREREG_PHASE_2_ADDENDUM.md` §2): the `full` fits keep the registered design; the `train` fits skip the Monte-Carlo p-value (E2.4(b) needs θ̂ only); the three sub-period fits drop to DE maxiter 15 and C = 50 and are reported as descriptive, with achieved counts and no significance claims. | Run the full design (25–35 h serially) or cut a sample silently | `PREREG_PHASE_2_ADDENDUM.md` §2 |
| P2-6 | Phase 1's `s_x_fit` (verification V6b) | **Corrected from 0.1286 to 0.0250.** `tools/phase1/e1_2_recovery.py::smm_fit` returns `s_x = sbar / 0.017 x 0.1752` — the calm engine's stationary sd(x) at the REFERENCE half-life of 150 d — applied whatever half-life the fit returned; estimator C's data fit returned h = 4.82 d, where the pull is thirty times faster. Re-measured by simulating that exact fit (sigma_V 0.01957, sbar 0.01248, h 4.824; 50 paths x 20,000 d): **sd(x) = 0.02498**, ACF(1) half-life 4.63 d. The corrected value agrees with estimator **A**'s independent 0.0237 [0.0216, 0.0258], which the published 0.129 did not — so A and C agree on all three quantities, not two. `value.json`'s `s_x_fit` now carries 0.0250 with the derivation and the previous value; `sigma_V` and `h` are unaffected, and `s_x_fit` is not a generator parameter, so no path changes and the execution-order rule is not triggered. Phase 1's report and P1-15 carry a "[Phase 2 correction]" marker. **Consequence the team must weigh:** the panel implies a mispricing amplitude of about **2.5 %** at a half-life of about **5 days**, against the environment's 17.5 % at 150 days — see PHASE_2_REPORT.md §5. | Leave the published 0.129 and let a wrong target propagate into Phase 2's sweep levels and into the deck | `e2_2/s_x_correction.json`; `e1_2/smm_fit.json`; `e1_2/decision.json` (estimator A) |
| P2-7 | The engine (E2.4; REG-4) | **`ar1_fit` — AR(1)+GJR-GARCH-t — is adopted**, by the pre-registered asymmetric rule applied unchanged. (a) **No engine is accepted**: J / df / χ²₀.₉₅ = 80.4 / 14 / 23.7 (`ar1`), 29.2 / 10 / 18.3 (`fw_v2`), 110.3 / 9 / 16.9 (`fw_plus`), and Franke–Westerhoff bootstrap p-values of 0.000 / 0.005 / 0.000 against a 5 % bar. (b) **Held-out prediction is a dead heat**: D over the eight persistence-carrying moments = 1.512 / 1.510 / 1.488 with MC intervals of ±0.18 that overlap completely, where a FW engine would have needed ≤ 0.512. Ties go to the simpler model. Three diagnostics make this more than a threshold technicality: fitted freely on 2000–2016 **`fw_v2` chooses to be an AR(1)** (α₀ 3.97, α_n 3.91 pin n_f at 1 on 100 % of days; its σ_V and sbar then match `ar1`'s to four significant figures and its J is 70.8 against 70.7); the full-sample `fw_v2` optimum has μφ = 0.31 where FW's own paper reports "around 0.01 or less"; and its remaining switching is a bistable herding lock (96 % of days at n_f > 0.99), not the alternation the narrative describes — while the variant that does alternate (`fw_plus`, 23.5 % chartists) fits worst of the three. Every document now names the engine **AR(1)+GJR-GARCH-t**. | Adopt `fw_v2` because it fits the full sample best (J 29.2 against 80.4). Rejected: it is not accepted, does not predict better out of sample, and the rule was written in advance precisely so that a better in-sample fit is not enough. **No threshold was moved.** | `e2_4/decision.{json,md}`, `engine_diagnostics.json`, `e2_3/smm_*_full.json`, `smm_*_train.json` |
| P2-8 | σ_V applied (D3 = (b)) | **σ_V = 0.01220 [0.01066, 0.01419]** (E2.3, 30 bootstrap refits), replacing v2's stipulated 0.006 in `value.json`. It was a free parameter of the SMM, jointly identified with the half-life, as the execution prompt's default option (b) requires. It is **below** Phase 1's estimator-C value (0.0196) and estimator A's (0.0205): the engine's structure moves it, which is exactly why option (b) fits them together. The execution-order rule was paid: path hashes, checklist and freeze regenerated on the handed-over state. | Apply Phase 1's 0.0196 unchanged — it was fitted under a different engine and a different moment set | `e2_3/smm_ar1_full.json`; `envs/v2/params/value.json` |
| P2-9 | `sbar` recorded, **not** applied | E2.3 fitted the unconditional x-innovation scale to **0.00870 [0.00659, 0.01298]** against v2's CAL 0.017. It is **not** written into the generator: `sbar` is a volatility parameter and PLAN §7 gives it to Phase 3 (E3.1 adopts the per-stock median unconditional sd, E3.4 decides how the regime enters the variance). Applying a Phase-3 parameter from a Phase-2 fit would pre-empt that phase and would change every magnitude item on the checklist. `params/mispricing.json` carries it under `applied.sbar_fitted_not_applied` with the reason, and the report states the consequence: the handed-over engine's sd(x) is the one E2.6 measures at sbar = 0.017 (0.034–0.105 over 200 days), not the one the SMM fitted. | Apply the fitted sbar now | `envs/v2/params/mispricing.json`; PLAN §7 |
| P2-10 | The E[x] known defect (items 4, 13) — **closed** | On the adopted engine E[x] in flat markets is **−0.00013 [−0.00087, +0.00064]** with jumps on and −0.00029 [−0.00102, +0.00046] with jumps off (1,000 flat paths each), against Phase 1's +0.0126 [+0.0038, +0.0214] which failed the ±0.02 margin by its upper limit. The bias was the FW recursion's own — its misalignment term is even in x while the herding lock is not — and an AR(1) has no such asymmetry. Registry entry removed; `test_flat_x_equivalence` is hard again, re-pointed at Phase 2's stored measurement and at `day >= 1` instead of the hard-coded burn-in index 260. | — | `e2_4/flat_x.json`; `tests/known_defects.py` |
| P2-11 | `test_fundamentalist_share` (items 2, 11) — **removed with a note** | The consequence PREREG_PHASE_2.md §6 fixed in advance for the AR(1) case. The engine that runs has no fundamentalist/chartist population, so a test of the share has no referent. What replaces it: `test_fw_units` locks the units bug fix that made the switching inert in the first place, and `e2_4/engine_diagnostics.json` records the realised chartist share of every fitted FW engine. Registry entry cleared. `test_half_life_consistency` is re-pointed: the AR(1) half-life is checked analytically against `mispricing.json`, and the 200,000-step pull-rate-vs-ACF(1) pilot check is kept on the `fw_fallback_hl150` sensitivity, which is what it was written for. | Keep a share test on an engine that has no shares | `tests/test_v2_1_stats.py`, `tests/known_defects.py` |
| P2-12 | The GARCH shape mismatch — registered to Phase 3 | E2.3 fitted the engine against E3.1's per-stock GJR-GARCH-t shape (α 0.027, γ 0.058, β 0.932, ν 4.86), as PLAN §6.2 prescribes, while the generator still runs v2's CAL shape (0.10 / 0.10 / 0.83 / 5). The engine's fitted parameters are therefore conditional on a volatility block Phase 3 owns and re-fits. Registered as a strict xfail (`tests/test_v2_1_phase_2.py::test_garch_shape_matches_e3_1`) with Phase 3 as owner, so the reconciliation cannot be forgotten. | Change the generator's GARCH here | `tests/known_defects.py`; PLAN §7 |
| P2-13 | The L2 shuffled-V control's evidence base | The bound (`max R2(shuffled V) < 0.1`) is **unchanged**; what changed is the panel it is applied to. On the 8-seed CI panel the statistic is 0.102 under the Phase-2 engine and 0.10 is not decidable there — a permutation across eight paths can align by chance — while on the published standard evaluation panel (1,600 paths, 288,000 rows) it is **−0.0023**, comfortably inside. The test now decides on the published audit and keeps the CI panel as a live range guard, which is exactly the arrangement `test_v2_L2b_phase_clock_selectivity` already documents for the same reason. Nothing was moved to make a result pass: the margin, the statistic and the estimators are identical. | Raise the 0.1 bound, or leave a CI test failing on a small-sample artefact | `e2_6_after/audit_after_levelfree.pkl` (`max_R2_shuffledV` −0.0023); `tests/test_leakage_ci.py` |
| P2-14 | The level-free / full-field leakage trade | Reported, not acted on. On the standard evaluation panel the handed-over state moves the **full-field** calm R²(x) from 0.823 to **0.346** and the all-phase value from 0.928 to **0.731**, while the **level-free** calm value moves from 0.086 to **0.339**; L2b's macro-phase selectivity worsens from +11.7 pp to **+15.4 pp** against its 10 pp margin (still a registered Phase-6 defect). The fitted engine writes a fast, small mispricing into recent returns, which a level-free reader sees, and hides it from slowly-updating valuation fields. The measured 0.339 [0.208, 0.434] sits above the Appendix-B bound at the parameters in force (0.245) but its interval does not lie entirely above it, so under Phase 1's own reading it is not a flagged excess. **Phase 6 owns the gate**; Phase 2 hands over the measurement and the bound, which now applies exactly because the adopted engine is literally the bound's model. | Treat the rise in level-free R² as a Phase-2 failure and re-tune the engine to suppress it — that would be tuning the generator to an audit statistic, which the plan's rules forbid | `e2_6_after/audit_after_levelfree.*`; `e2_4/kalman_bound_phase2.json`; `e1_6/audit_after_levelfree.*` (Phase 1's row) |
| P2-15 | How the two acceptance criteria are read (correction to this phase's own report) | **My earlier reading of the sub-period cells — "not rejected on 2009–2016 or 2017–2024" — is withdrawn.** The two criteria are different tests and only the bootstrap one is ever passed; the column is renamed from "accepted" to "FW bootstrap: not rejected?" in the report and in every generated table. Franke–Westerhoff's statistic carries **no degrees-of-freedom penalty**, is evaluated **in-sample at that window's own optimum**, and — decisively — **its yardstick moves with the window length**. Measured (`e2_3/weight_calibration.json`): the data bootstrap's own J median is 14.2 on `full` and 15.0 on `train` against a chi-square(17) median of 16.3 (ratios 0.87 and 0.92, well calibrated), but **40.9 / 44.0 / 35.2 on p1 / p2 / p3** (ratios 2.50 / 2.69 / 2.16), so Sigma-hat understates the moments' sampling variability at ~2,000 days and the yardstick inflates from 26.7 to 54–66. p3's J = 44.7 clears a bar it would fail by a factor of two on the full sample. **The honest statement: no window passes chi-square on any engine, and the sub-period bootstrap p-values are not evidence of acceptance because their own null distribution is mis-calibrated.** What the sub-periods do support is unaffected: the half-life is 13.1 / 8.9 / 9.2 / 7.5 / 5.2 d across every window, a point estimate that does not depend on the weight matrix's calibration. | Leave the "not rejected" reading standing — it is the one thing in the report a hostile reviewer could quote against it | `e2_3/weight_calibration.json`; `e2_3/smm_ar1_*.json` |
| P2-16 | sigma_V is engine-conditional, and the label now says so | Phase 1's estimator C returned **0.01957 [0.01892, 0.02036]** and estimator A **0.0205 [0.0197, 0.0213]** on the same panel and the same moment family; Phase 2's SMM returns **0.01220 [0.01066, 0.01419]**. The intervals do not overlap, so **sigma_V is not identified independently of the engine** and a bare FIT label implies more than the evidence supports. `value.json`'s label now reads "FIT, CONDITIONAL ON THE ENGINE", records both Phase-1 estimates and the fit's other conditions (E3.1's GARCH shape, held fixed and re-fitted by Phase 3), and states that the value is **expected to move again** at Phase 3; `mispricing.json`'s `structural` and `half_life` entries carry the same qualification. Quote it as "sigma_V of the adopted engine", never "the panel's sigma_V". | Keep the bare FIT label and let Phase 6 or the deck quote 0.0122 as the panel's sigma_V | `envs/v2/params/value.json`; `envs/v2/params/mispricing.json`; `e1_2/decision.json` |
| P2-17 | E2.7 (post-review): does the benchmark still discriminate? | **It does.** On the handed-over state the 16A G1 ordering — MCR(mandate oracle) < MCR(observables oracle) < MCR(best trivial policy) — holds in **all four scenarios**, with both gaps significantly positive at 95 % (paired cluster bootstrap over runs, 40 training seeds disjoint from 50 scored seeds, three personas). What the fitted persistence costs is the spread's **size** where coverage falls: the L5-to-trivial gap goes 0.0434 → 0.0289 in flat (−33 %) and 0.0318 → 0.0171 in sustained bull (−46 %), while crash (+12 %) and bull trap (−2 %) are unaffected. A second cost surfaced by the run: **MCR is undefined on runs with no resolvable step** — 4 % of flat runs and 10 % of sustained-bull runs against 0 % and 6 % before — a Phase-7 specification question raised in the report's §5. No pass/fail is applied: G1 is Phase 6's gate on the Phase-6 frozen generator. | Hand the team the coverage collapse (0.73 → 0.13) without the number the decision turns on | `e2_7/l5_phase2_after.csv`, `e2_7/discrimination.{json,md}`; `PREREG_PHASE_2_ADDENDUM.md` §4.1 |
| P2-18 | E2.8 (post-review): the level-free surrogate is **not** biased | Because the adopted engine IS Appendix B's model, the bound is exact, and the surrogate sitting above it had only two explanations: a biased surrogate (which would affect every level-free leakage number in the programme) or an unmodelled channel. Simulating the bound's model and nothing else, with the SAME features, estimators and cross-validation, the surrogate measures **0.241 [0.214, 0.268] against an analytic bound of 0.245** at the parameters in force, and sits at or below the bound at two contrasting configurations spanning a bound range of 0.09–0.25. **Explanation (a) is refuted**; the generator's 0.339 is a real channel worth about **+0.10 of R²**, contributed by what the exact process lacks — the scripted events and their drift, the jumps, the GJR innovation and the sentiment feedback. Two corroborations fall out: the v2-state row (0.091) reproduces Phase 1's SEP after-state level-free calm R² of 0.086 by a completely different construction, and the ordering across configurations tracks the bound's. Phase 6 inherits a **characterisation** problem, not a validation one. | Defer the question to Phase 6 and build a gate on a statistic whose validity was unknown | `e2_4/bound_check.{json,md}`; `PREREG_PHASE_2_ADDENDUM.md` §4.2 |
| P2-19 | E2.6 extended to the levels inside the fitted interval | Levels **5, 10 and 15 d** added to both matchings at the unreduced 200-seed switch statistics, because the adopted half-life's interval is [3.81, 23.61] d and the original sweep had one point inside it. Result: the switch counts hold across 5–10 d (median 2–4, 59–85 % of runs at ≥ 2 in flat, crash and bull trap) and fall to a median of 1 only at 15 d under matched sd; the bull-trap rejection rate falls monotonically with the level (0.97 / 0.94 / 0.89 / 0.50–0.64 at 5 / 7.5 / 10 / 15 d). The calibration was **extended, not recomputed** — the stored target sd(x) is kept and the extension's seeds are keyed on the level value, so no cell already run moved; the calibration table marks each level's pass. The level-free audits were not extended. | Leave a robustness claim resting on the point estimate alone | `e2_6/sweep.{json,md}`; `PREREG_PHASE_2_ADDENDUM.md` §4.3 |

## Phase 3 of the v2.1 programme (2 September 2026): volatility — GARCH, jumps, regime variance and implied volatility — decisions

Governing documents: `docs/env_v2/v2_1/PREREG_PHASE_3.md` (written before any run), `PREREG_PHASE_3_ADDENDUM.md`
(three designs found wrong at their first measurement, each corrected in a documented step before the run it
governs, with disclosure and results under both rules), `PHASE_3_REPORT.md`. Team decisions applied: D1 = C,
D13 = mechanism C, D16 = full programme; the Phase-2 §5.1 blank is empty, so Phase 3 ran on the environment as
handed over. Every result produced on the local reference machine; nothing offloaded. Nothing committed; every
entry names its evidence file under `generated/v2_1/`.

| # | Topic | Decision | Alternative | Rationale / evidence |
|---|---|---|---|---|
| P3-1 | The GARCH shape (E3.1) | **alpha 0.027 [0.025, 0.030], gamma 0.058 [0.055, 0.061], beta 0.932 [0.928, 0.935] adopted (FIT, set-A full-sample per-stock medians)** — the plan's own rule; confirmed by recomputation from `garch_fits.csv` before use (V1). Sensitivity sets P25/P75 built on quantiles of (alpha, gamma, nu, persistence) with beta derived, because the raw per-parameter P75s give persistence 1.028 (nonstationary). | Adopt a sub-period set (persistence runs 0.946–0.994): rejected — the full-sample median is the registered rule and the sub-period story is reported, not fitted to | `e3_1/summary.md`, `e3_1/confirm.json` |
| P3-2 | The three volatility scales named, and sbar's identity (PREREG 3.4) | **sbar is FIT by the variance-accounting identity**: the generator's free-running unconditional daily return sd equals E3.1's 0.0218 [0.0210, 0.0225]; sbar² = (s_A² − σ_V²)(1+ρ)/2 − λσ_J², imposed inside the §9 refit so the final triple satisfies it exactly. WHICH IS WHICH: 0.0218 = the panel's TOTAL-return unconditional sd; E2.3's 0.0087 = an engine-conditional x-innovation scale under a 17-moment objective whose volatility block the model misses by −3.5 sd (not a measurement of the panel's volatility); 0.017 = v2 CAL, superseded. | Adopt E2.3's 0.0087 (halves sd(x), collapses coverage further, and imports a rejected model's compromise); or keep CAL 0.017 (stipulated) | `e3_1/summary.md`; `e2_3/smm_ar1_full.json`; PHASE_3_REPORT §3.1 |
| P3-3 | The run-up calm reference (ADDENDUM §1) | **The multiplier reference is the stock's unconditional level** (median rolling RV21): the registered run-up pre-event window sits at 2.74× [2.57, 2.89] unconditional (a post-crash trough — 2003/2009/2020 bottoms), which made every run-up multiplier < 1 against a reference that is not calm; the drawdown window sits at 1.01 [0.98, 1.06], validating that only the run-up family needed the fix. Both variants published side by side. | Keep the registered reference and adopt m_mania = 0.46 — the generator's mania phase would carry LESS variance than calm, contradicting GSY's read direction because the denominator measures the wrong thing | `e3_3/episodes.md` (both columns); ADDENDUM §1's disclosure |
| P3-4 | The E3.4 target population (ADDENDUM §2) | **The fast-crash subpopulation (peak→trough ≤ 126 trading days; threshold from the generator's own template length, fixed before the subpopulation was computed) governs the mechanism adoption**; the pooled ≥ 30 % population stays the registered primary and both verdicts are reported. Measured: pooled rise 118 d [107, 131] (dominated by multi-year declines the scripted crash does not model — under the registered rule the verdict would be decided by Phase 4's schedule template, not the variance mechanism); fast-crash rise 30 d [29, 35], decay 9 d [9, 10], n = 642/531 episodes. | Judge on the pooled CIs alone (no mechanism can reach 118 d inside a ≤ 110-day scripted event; the rule would be vacuous) | `e3_3/episodes.json`; `e3_0/power.json` PA4 |
| P3-5 | The jump block (E3.2; ADDENDUM §3) | **The mixture fit's triple with recovery-informed widened intervals**: ν 6.650 [6.48, 6.83] (diffusive, net of jumps; recovery-certified ±2.4 %; E3.1's QML 4.86 is the TOTAL tail, recorded beside), λ 0.000583 [0.00046, 0.00089] (three estimators agree on the rate: mixture 0.00058, excess-rate 0.00074 [0.00060, 0.00089], size-arithmetic 0.00074 — ≈ 0.15/yr), σ_J 0.230 [0.086, 0.241] (WEAKLY IDENTIFIED, factor ≈ 2.7 across estimators; robust joint statement: rare and very large). The model as a whole is REJECTED (J 790: pooled-curve ν heterogeneity + announcement elevation beyond a total-rate-preserving split) and the label says so. v2's CAL (0.010/day, σ 0.03) superseded — fourteen times the rate at a seventh the size. placement x_zero and q = 0.4345 kept (E1.4 FIT); p_ann/lam_res re-derived from (λ, q). | (a) The registered fallback (ν pinned 4.86): measured DEGENERATE — λ → 3·10⁻¹⁴ at J 1455, because the pinned total-tail over-explains the middle of the curve; adopting it would write "no jumps" against a robustly positive excess (0 excluded at every θ ≥ 3.5). (b) Keep CAL: no data support. | `e3_2/{detection.md,fit.json,fit_fallback.json,recovery.json,size_decomposition.json,adoption.json}`; ADDENDUM §3 |
| P3-6 | The IV premium and noise (E3.5) | **Constant premium adopted by the leave-one-name-out 1-SE rule**: log(1+π) = −0.0224 (CV MSE M0 0.04899 ± 0.00862 vs M2 0.04838; per-name slopes sign-unstable −0.096…+0.041, so a level-dependent premium does not transfer across names — Carr & Wu's single-stock finding); ε AR(1) with ρ 0.926 (range 0.923–0.931) and innovation sd 0.083 (0.058–0.098), from the new day-indexed `iv` stream. The CAL floor 12, the stress trigger and the whole-path quantile are REMOVED; the pooled CBOE minimum 5.13 is the anchor. Five mega-caps, stated wherever quoted. | Adopt M1/M2 (better raw CV but inside one SE and name-unstable — the 1-SE rule was registered precisely for this) | `e3_5/premium.{json,md}` |
| P3-7 | The engine refit (PREREG §9; closes P2-12) | **One constrained re-launch of Phase 2's SMM on the Phase-3 volatility block** (E3.1 shape + E3.2 jumps in the simulator; sbar eliminated by P3-2's identity; free = (σ_V, h), df 15; fresh seed blocks SM3/SB3/MC3; decided and registered before any Phase-3 fit): **σ_V 0.01457 [0.01275, 0.01527], h 22.38 d [18.75, 32.64] adopted**; sbar 0.01509 by the identity; J 119.3, not accepted (no engine of this class ever has been on this panel) with the rejection now resting on the long-memory clustering moments (vMean residual +0.8 vs Phase 2's −3.5 — the identity pinned the volatility level as designed). Phase 2's h = 7.50 lies OUTSIDE the new interval: the conditionality P2-12/P2-16 registered is now quantified. The unconstrained sensitivity (Phase 2's exact parameterisation under the new block: σ_V 0.01176, sbar 0.00991, h 16.96, J 93.1) is published beside, so the shape effect and the constraint effect are separable. V4: a 50 × 20,000-d free run reproduces s_A to 0.02 %. V7: E2.5's rows re-run at h* (naive T=200 median 16.0 d). The engine's identity (`ar1_fit`, E2.4) was NOT re-opened. | (a) keep Phase 2's (σ_V, h) and record the conditionality as a permanent caveat — rejected: the provenance would no longer hold (the shape it was fitted against is not the shape in force); (c) run both as a factor — rejected as unnecessary once (a′) costs one cell | `e2_3/smm_ar1c_full_p3.json`, `smm_ar1_full_p3u.json`; `e3_4/{block.json,v4_uncond_check.json}`; `e2_5/hl_at_h22.json` |
| P3-8 | The multipliers in force (E3.3 → generator; PREREG 5.3) | **Closed-loop x-innovation multipliers whose generator-realised total ratios match E3.3's FIT medians**: m_x = deterioration 1.35, panic 16.17, stabilisation 5.63, mania 1.35, post-top 0.35 (calm and sustained-bull 1.0, DESIGN R1-D5). Verified at 200 seeds: realised 1.34 / 7.54 / 3.12 / 1.19 — all inside the target CIs. Two structural shortfalls documented on the file: the blow-off label is EX POST so its multiplier is inert (v2's 2.0 was dead code — surfaced by the diverging calibration knob; recorded at mania's value; the empirical 1.65 target is Phase 4's) and post-top is drift-dominated (realised floors at 1.59 vs target 1.16 whatever the innovation multiplier; the drift shape is Phase 4's E4.8). | Adopt E3.3's total ratios directly as x-multipliers (the first-order mapping alone): the realised panic ratio would land at ~2.4 — v2's own silent gap between nominal ×5 and realised ×2.4 measured in PA3 | `e3_4/calibration.json` (history + verify), `e3_3/episodes.json` |
| P3-9 | The variance mechanism (E3.4; REG-6, A3 revisited) | **A (whole-variance scaling) stays, as REG-6's documented-shortfall consequence**: no mechanism lands the rise time inside either empirical CI (A 75.5 / B 103 / C 80 d vs fast-crash [29, 35] and pooled [107, 131]) because the onset-to-RV-peak time is set by the SCHEDULE template (deterioration + panic build-up), not by how variance enters — even the instant multiplier cannot rise faster than the scripted price falls. B (ω + FIT ramp 20 d) nears the pooled rise (103 vs 107) but censors 35 % of decays past 250 d; C (two-regime switching, v_ratio 16.2, p_exit 1/64, entries derived from the multipliers, sbar rescaled 0.672 to keep the identity) rises no faster and decays slower (14 d). All three arms published; the rise-time tension handed to Phase 4 with the numbers. All three mechanisms remain engine options behind `scale_mode`. | Adopt B for its pooled-rise near-miss (fails decay censoring and the governing fast-crash rule); adopt C for its structure (fails both statistics with more parameters) | `e3_4/{decision.md,arms.json,mechanism_c.json}`; ADDENDUM §2 |
| P3-10 | Compute placement (Kaggle) | **The cross-machine reference row is PROVEN for the first time** (kernel fp-p3-refrow: worst relative difference 6.4e-16 on all 51 moments, numpy 2.0.2/scipy 1.16.3 vs local 2.4.6/1.18.0), and the E3.4 simulation stages ran on the proven kernel (fp-p3-e34, bundle finpersona-phase3-bundle — code + cached derived statistics only; `datasets/` never uploaded). Every audit, L5 and surrogate number remains local-only (the Phase-1 non-reproduction rule). | Run everything locally (Phase 2's choice): equivalent for validity; the kernel freed the laptop for the serial audit chain | `e3_0/power.json` (kaggle_reference_row); kernel logs |
| P3-11 | The IV construction in force, its audits and the derived tolerance (E3.5; items 46, 25) | **IV_t = sqrt(252 fc_t)(1+pi)e^(eps_t)** — fc from a PAST-ONLY GJR filter on observed returns (omega anchored at the identity's unconditional variance), pi the constant FIT −0.0224 (P3-6), eps AR(1) FIT from the day-indexed `iv` stream. Removed: the stress decile trigger, the whole-path quantile (the look-ahead), the sigma_V add-on, the w² factor (V5: it silently inflated the AR(1)-era IV level ×1.19 — a surfaced plumbing residual), the CAL floor 12. Audits at 200 crash seeds: transition z = +0.12 (was ≈ 7); T_z DERIVED = 2.70/2.28/1.88 (P95 of the same filter's forecast z, written into volatility.json); onset ΔAUC = −0.107 vs null P95 +0.032 (IV carries LESS phase signal than the leak-free reference); test_iv_no_lookahead bit-identical prefixes. `test_iv_continuity` HARD and passing; both registry entries removed. | Keep a floor/stress premium (a clip and a near-clock); derive T_z from IV's own distribution (circular — the reference must be the legitimate channel) | `e3_5/audit.{json,md}`; `e3_5/premium.json`; `envs/v2/observables.py::iv_block_v21`; tests |
| P3-12 | The calm level-free channel: **this phase's own conclusion withdrawn** (post-review, E3.9a) | **"The calm price channel is gone" is WITHDRAWN.** The published calm R²(x) of −0.58 comes from `l2_surrogate`, which fits on ALL rows (GroupKFold by path) and masks by phase group afterwards — a cross-phase-trained model scored against calm-only variance, and Phase 3's event phases move x about twice as far as Phase 2's, which is what drove it negative. Re-fitting the identical machinery (same features, three estimators, GroupKFold, 500-resample cluster bootstrap) on **calm rows only**, on both phases' stored hand-over panels: level-free calm R²(x) **0.421 → 0.349**, sign accuracy **0.892 → 0.814**. The channel **narrowed modestly; it did not close**. Two corrections follow: §3.10's reading that the Appendix-B question "dissolves" is withdrawn (the excess over the bound is ≈ **+0.19**, 0.349 against 0.163 — larger than Phase 2's +0.10), and the decomposition now closes on one footing (E3.8's calm-only arms reach 0.203, so ≈ +0.15 is events-and-sentiment on calm rows) — the characterisation problem P2-18 handed Phase 6, posed on a consistent statistic. The published cross-phase statistic is NOT changed: it is what Phase 6's gate will be written on, and both trainings are now measured on both states. **The sign column, which was in the CSV and not in the report, is now beside the R² column** — at 0.722 the published statistic is still above the plan's own 0.70 L2 sign threshold, which the "channel is gone" reading had missed. | Leave the reading standing on the statistic that flattered the phase; or silently swap the audit's estimator (which would break every before/after row in the programme) | `e3_9/calm_trained.{json,md}`; `e3_after/audit_after_levelfree_L2.csv`; `PREREG_PHASE_3_ADDENDUM.md` §4.1 |
| P3-13 | `sbar`'s interval — a registered deliverable delivered late (post-review, E3.9b) | PREREG §3.4 registered a Monte-Carlo propagation for `sbar` and the phase did not produce it: `volatility.json` carried `"interval": null` on the block's most-used number and the report's §4 table printed **s_A's** interval in that column. Delivered at 20,000 draws over the registered inputs: **sbar = 0.01509 [0.01342, 0.01678]**, with **0 %** of draws in the identity's failure region. Stated limitation, on the file: the refit stored per-parameter percentiles and sds rather than the 30 paired draws, so (σ_V, h) are reconstructed as CI-clipped normals matched to the stored sd under both an independent and a rank-correlated pairing (they agree to the fourth decimal; the wider is adopted); s_A and (λ, σ_J) are resampled from their stored draws exactly as registered. | Leave the entry without an uncertainty, or quote s_A's interval as if it were sbar's | `e3_9/sbar_interval.json`; `envs/v2/params/volatility.json` |
| P3-14 | The level anchoring **double-counts the panel's crises** — confirmed, reported, not repaired (post-review, E3.9c) | The identity pins the generator's free-running (calm-labelled) return sd to the panel's **full-sample** unconditional 0.0218; measured, the panel's own crisis-free calm is **0.0170 [0.0163, 0.0176]** (√ of the median pre-event 120-day `rv_calm`, 1,592 episodes / 412 stocks), so the target is **1.28×** the panel's calm. ADDENDUM §1 then re-based the multipliers to that same all-day level, so the event variance stacks on a calm level that already contains it. The deployed check (events on, 200 seeds each over the audit scenario mix) gives a realised pooled daily return sd of **0.0284 [0.0267, 0.0302]** against the 0.0218 target — **+30.5 %**, against a bootstrap half-width of 0.0017. Neither V4 (free-running, no events) nor E3.4's rise/decay rule (a shape test) could detect it; PREREG §3.4 named the worry and assigned it to the wrong instrument. Reported as a defect on `volatility.json`'s `sbar` label and put to the team with three costed options (PHASE_3_REPORT §5.7); **not repaired here**, because re-choosing the anchor means paying the whole execution-order cascade again. The registered calm-window mapping, also missing from the report, is delivered with it: its sbar would be **0.00676** (0.45× the adopted). | Repair it unilaterally (a full re-run of a frozen state on a judgement the team owns); or leave the registered alternative unreported and the worry assigned to an instrument that cannot see it | `e3_9/level_check.{json,md}`; `PREREG_PHASE_3_ADDENDUM.md` §4.3 |
| P3-15 | What the weakly-identified jump size costs, and which way it errs (post-review, E3.9d) | σ_J is identified only to a factor ≈ 2.7 and is not inert (it enters the identity and E3.8 attributed the only readable volatility channel to it), so the span across the adopted interval was measured, holding λ and re-deriving sbar from the identity at each σ_J so only the jump/diffusive split moves: level-free calm R²(x) **0.158 / 0.176 / 0.229** at σ_J **0.086 / 0.230 / 0.241** (jump share of the x-innovation variance 1.7 / 11.9 / 13.1 %), intervals overlapping. **The weak identification costs about +0.018 R² from the interval floor to the adopted point** — materially less than feared, and the expectation that the channel "largely disappears" at the floor is not what the measurement shows. What the run settles is the direction of the error, which "weakly identified" did not state: **the adopted 0.230 is the conservative end — it overstates the jump channel rather than hiding it**, and the parameter file now says so. The adopted point does not move (it is the registered instrument's optimum and the state is frozen). | Move the adopted point to the floor or the mid-interval after seeing the leakage consequence — which would be tuning a generator parameter to an audit statistic, exactly what the plan's rules forbid | `e3_9/jump_sensitivity.{json,md}`; `PREREG_PHASE_3_ADDENDUM.md` §4.4 |
| P3-16 | Six checkable errors in the Phase-3 documents, corrected in place (post-review) | (1) **λ's interval was miscopied in three documents** — the θ = 4 excess rate is [+0.00066, +0.00082] (0.00089 is θ = 4.5's upper) and the adopted union is [0.00046, **0.00082**]; `volatility.json` carried the right numbers throughout, the prose did not, on the one parameter whose entire label is "weakly identified". (2) The report's header **contradicted itself on compute provenance** one paragraph apart (local-only vs the Kaggle arms); the Kaggle statement is correct and both now say so. (3) §3.3 quoted the run-up count as 3,125/394 when **3,202/398 qualify** (3,125/394 is the multiplier-usable subset), and `volatility.json`'s `phase_multipliers.n` recorded the n of none of its own values — now the per-family episode and stock counts. (4) Post-top's realised 1.59 now carries its **n = 20**, since it drives a Phase-4 hand-off. (5) `value.json`'s `h_fit` label was **two phases stale** (it still said 150 d in force and "Phase 2 reconciles them") in a file this phase edited. (6) `known_defects.py`'s L2b entry still quoted **+11.7 pp** against the +12.7 pp in force. All are label/prose corrections: no generator path changes, verified by regenerating the 95-configuration path hashes and comparing (P3-17). | Leave them: each is small on its own, and each is exactly the kind of number a later phase or the deck would quote | `PHASE_3_REPORT.md` §3.11; `e3_2/detection.json`; `e3_3/episodes.json`; `e3_4/calibration.json` |
| P3-17 | The freeze after the post-review provenance edits | The corrections of P3-12 ... P3-16 touched two files under the freeze manifest (`value.json`'s stale `h_fit` label, `volatility.json`'s `sbar` interval and level caveat, the `jumps` span, the `phase_multipliers` n record) -- all **labels and provenance, no values the generator reads**. Following P2-16's precedent the manifest was rewritten (label "v2.1 Phase 3 freeze (post-review)", 23 files, hash bac2a8a2) and the 95-configuration path hashes were **regenerated and compared**: **0 of 190 hidden columns changed** (0 non-analyst, 0 analyst), which is the evidence that the edits moved no path. | Edit provenance in a frozen file without re-hashing, and ask the reader to take it on trust | `path_hashes_phase3_after_postreview.json` vs `path_hashes_phase3_after.json`; `tests/v2_freeze_manifest.json` |

## Phase 4 — events, schedule, controls and the calendar (v2.1)

| # | Decision | Evidence | Alternative rejected | File |
|---|---|---|---|---|
| P4-1 | **Phase 3's level double-count is WITHDRAWN as a level defect and re-expressed as a shape finding** | Measured conditional on phase, with one estimator on both sides (generator clustered by seed, panel by stock): the generator's pooled calm sd is **0.022007 [0.021618, 0.022441]** against the panel's **0.021672 [0.020860, 0.022439]** — ratio 1.015, inside the CI — with the panel's own full-sample `s_A` (0.021793) sitting between them. Phase 3's 1.28× compared a *median-across-episodes* panel statistic with a *pooled-mean* generator statistic; the per-stock ratio of those two aggregators is **1.354 [IQR 1.286, 1.452]** (n = 417), which is the whole of the gap. The deployed "+30.5 %" is the benchmark's own scenario mix: it pools flat / crash / bull-trap / sustained-bull with equal weight, and the generator spends 5.3 % of its days in panic at about 8x variance where no all-day panel does; re-weighted to the panel's phase-day weights the excess falls to +15.3 %. Crash-side ratios to own calm, panel vs generator: panic **7.642 vs 8.594**, stabilisation **3.109 vs 3.418**, deterioration **1.618 vs 1.414** — all three just outside the panel CI under the strict point-in-interval rule, with the generator's own CIs overlapping the panel's in every case. **The anchor is KEPT**: no re-anchoring, no engine re-identification, no execution-order cascade. | Re-anchor `sbar` to 0.00676 (Phase 3's option (b)), which Phase 3 measured as halving flat sd(x) from 0.0468 to 0.0214 and undoing the coverage gain — paid to fix a defect that the matched estimator says is not there | `e4_0/phase_levels.{json,md}`; `e4_0/level.json`; `PREREG_PHASE_4_ADDENDUM.md` sections 1-2 |
| P4-2 | The deterioration phase is too quiet, and that is E4.2's problem not the level's | Under the matched estimator the generator's deterioration realises **1.414 [1.253, 1.604]** of its own calm against the panel's **1.618 [1.506, 1.746]** — the one crash phase that is too *cold* while panic and stabilisation are slightly hot. Handed to E4.2/E4.6 as a shape problem. | Treat it as a level error and rescale the block, which would move panic and stabilisation the wrong way | `e4_0/phase_levels.md` |
| P4-3 | **D4 applied: the single-stock panel is primary and the index tables cannot substitute** | E4.1 computed both. The panel gives 3,078 drawdowns of 20 % or more and **1,789 of 30 % or more** over 417 stocks (reproducing Phase 3's inherited counts exactly), plus 1,975 / 4,535 Pagan-Sossounov bear phases at two censoring settings and **3,202 run-ups** over 398 stocks. The Shiller real-price series, computed at first hand over 1871-2026, yields **12** episodes of 20 % or more and **6** of 30 % or more — n far too small for a P10/P90 on any quantity, and monthly, so it cannot supply a deterioration length or a 60/120/200-day recovery share at all. | Set the sampling ranges from the index tables, or blend the two populations | `e4_1/episodes4.json`; `e4_1/index_shiller.csv` |
| P4-4 | Mishkin-White and Barro-Ursua are **not quoted**, and Pagan-Sossounov's durations are not either | Neither NBER paper was read at first hand in this phase, so under the plan's Section 1 rule no number from either appears in any table, tolerance or parameter file; the index table published beside the panel is this phase's own computation on the Shiller series it holds. Pagan and Sossounov's algorithm is applied; their 25/15-month durations were not read and are not quoted — the daily censoring constants (168/126/336 and 84/63/168 trading days) are stated as this phase's own choices and the whole table is reported at both. | Quote the published crash counts from the plan's summary of them, which is a secondary source | `e4_1/episodes4.json` `index_shiller.citation_status`; `tools/phase3/episodes.py` `PS_SETTINGS` |
| P4-5 | **The schedule's ranges are FIT, and "drawn from the empirical P10-P90" means the empirical distribution, not a uniform over it** | The panel's fast-crash family (peak-to-trough at most 126 d; **642 episodes / 313 stocks**, reproducing Phase 3's own n and its 30 d [29, 35] rise time) gives `det_len` P10/P50/P90 = **3 / 8 / 25.9** where v2 drew U(15, 40), and `panic_len` **15 / 39 / 100.9** where v2 drew U(15, 70). A uniform over [P10, P90] reproduces the interval but replaces the fitted *shape* with a stipulated one and mis-centres every right-skewed parameter by 45-80 % (`panic_len` median 57.9 against the panel's 39) — the interval would be FIT and the distribution DESIGN, which the governing rule forbids. Sampling is by inverse CDF from a 33-point quantile grid. Measured effect on the rise time: v2 70.0 d, uniform 65.0 d, **empirical 58.0 d**. | Keep the uniform reading, which the smoke run showed buys one day of rise time because it puts the mass back where v2 had it | `e4_2/schedule.{json,md}`; `PREREG_PHASE_4_ADDENDUM.md` section 3 |
| P4-6 | **The rise-time criterion is NOT MET, and Phase 3's diagnosis of which lever owns it is corrected** | At 500 crash seeds with the FIT empirical ranges the generator's median onset-to-RV21-peak rise is **58.0 d [52, 64]** against the panel's **30 d [29, 35]** — an improvement from 70.0 d [65, 74] but not an overlap, so the criterion is recorded as **not met** and not relaxed. What binds is not the deterioration length: `corr(det_len, rise) = 0.140` and `corr(panic_len, rise) = 0.121`, against **`corr(onset lag, rise) = 0.684`**. The episode's running peak falls a median of 38 days *before* the scripted event, so ordinary calm-phase noise dates the onset. On the **167 of 500** paths whose onset coincides with the event the generator gives rise **32 d** and duration **54 d** against the panel's 30 and 53 — the schedule itself is right. The binding parameter is `setup_len`, which is E4.7's, and the rise-time question is handed there. | Report "the schedule owns the rise time" as Phase 3 framed it and shorten `det_len` further, which the correlations show buys almost nothing | `e4_2/schedule.md`, section "What actually binds the rise time" |
| P4-7 | **The hazard is re-fitted and REG-18's mapping A adopted — the unique arm inside the panel's CI** | The panel's own topped share (a 40 % or deeper drawdown within 200 trading days of a run-up top) is **0.110 [0.0966, 0.1245]**, n = 3,201 run-ups / 398 stocks — so REG-18's rule is decidable and its n below 30 fallback is not needed. `b` = **5.419** from a logit through GSY's three published points (read; it reproduces their 20/53/80 %); `h0` is solved from a hazard-off pilot rather than stipulated. Realised topped shares: **A (no scaling) 0.108 [0.081, 0.135] — INSIDE**; B at 0.5x/1x/2x 0.022/0.034/0.048; C (panel-fitted b = 1.074) 0.036; v2's incumbent 0.064 — all OUTSIDE. The **40-60 % topped band and the P/V 1.6-2.5 band are retired as targets**; peak P/V is reported as an outcome (median 1.49). A measured fact for the team: the industry-level slope is **5.1x steeper** than the single-stock one, which is what REG-18's transfer assumption has to carry. | Keep v2's CAL (h0 3e-4, b 6.0), which was grid-searched against an engine whose x moved four times as far and now sits outside the panel CI; or adopt the panel-fitted mapping C, which fires three times too rarely because the generator's manias reach 1.63x where the panel's run-ups reach 2.23x | `e4_3/hazard.{json,md}` |
| P4-8 | **A super-exponential mania is contradicted by the panel on two independent measurements; kappa is reported, not applied** | The registered LPPLS trigger fails: the stable share is **0.329 [0.313, 0.344]** against the required 0.50 or more, over 3,125 converged Filimonov-Sornette fits on 398 stocks (median m 0.926 [0.900, 0.953] — inside (0,1) with a CI excluding 1.0, but close to the exponential boundary). The registered fallback — the scripted drift with its shape FIT from the run-up table — returns **kappa = 0**: the panel's run-ups *decelerate*, with a median last-third-to-first-third log-gain ratio of **0.665** over 2,937 run-ups. kappa = 0 is **not applied**, because the run-up definition selects on a local maximum at the top, which mechanically flattens the final segment, and that selection must be settled before a value that changes what the bull-trap scenario *is* is adopted. Amendment A5 is revisited on this evidence: the cap cannot be retired on the LPPLS route because the route is unsupported, and it is "unnecessary" under kappa = 0 only in the degenerate sense that nothing compounds. | Adopt kappa = 0 on the fit alone, silently turning the mania into a linear drift; or relax the stability clause to reach the registered route | `e4_1/lppls.{json,md}`; `e4_4/mania.{json,md}` |
| P4-9 | **The control: all four REG-7 definitions audited, none adopted — D14 stands open, and `test_sustained_bull_selection` is re-registered** | At 500 seeds per definition, generated with rejection **off** so the accepted and rejected populations are both observable (v2's internal retry loop hid them). Selection, accepted vs rejected: the anchored incumbent **C** has KS **0.474** on daily sd and **0.434** on mean IV; **A/B/D** have **0.081** and **0.187**. Realised sd(x): A/B/D 0.0467, C 0.0234; rejection 16.2 % vs 35.8 %. Discrimination against a **refitted** seed-permutation null: all four at chance (A/B/D 0.5360 against null p95 0.5390; C 0.6058 against 0.6065). The registered equivalence bound (bootstrap upper limit below 0.10) is **UNDECIDABLE** at the achieved rejection counts — two samples drawn from the *same* distribution at n = 419/81 give an upper limit of **0.200** — so the comparison between definitions carries the finding, not the bound; the remedy is more seeds, and it is stated. The v2 `V_T/V_1 >= 1.2` threshold is stipulated; its FIT replacement is **1.3233** (P10 of the 200-day-equivalent growth of 2,849 non-crashing run-ups), recorded but not applied while the definition itself is undecided. | Adopt A, B or D on the audit evidence — which the pre-registration explicitly forbids, because D14 is a statement about what the control is *for* and the audits cannot supply it | `e4_5/control.{json,md}`; `tests/test_v2_1_phase_4.py::test_sustained_bull_selection` |
| P4-10 | **Event dynamics: all four formulations run at 1,000 + 1,000 seeds, none qualifies, the phase STOPS for D5** | Coverage of the panel's joint depth-and-duration P10-P90 box: A at lambda 0.02/0.05/0.10/0.25 gives 0.474/0.480/0.520/0.562; B 0.521; C 0.462; D 0.323. All below the registered 0.70; none straddles it at this n, so none is "undecided". Rejection is below the ceiling (0.357) everywhere, so coverage alone binds. Script share, measured from the **recorded** drift `PathResult.drift`: 0.047/0.062/0.080/0.130 for A (monotone in lambda, which is the instrument's validity check), B 0.039, C 0.014, D 0.030. **The rule's premise is arithmetically wrong**: REG-8 justifies the 0.70 threshold as a margin below "0.80 by construction", but a P10-P90 box on *two* quantities captures about 0.64, and the panel's own self-coverage is **0.643** (n = 642) — the registered rule asks the generator to beat the panel by 6 pp, so no result could have met it. Phase 4 reports both the verdict and the arithmetic and does **not** set a corrected threshold. | Set a corrected margin after seeing the table and adopt the winner under it — moving a threshold after data, and taking D5 on the team's behalf | `e4_6/dynamics.{json,md}`; `PREREG_PHASE_4_ADDENDUM.md` section 4 |
| P4-11 | **The blow-off label is given a real-time criterion — the dead multiplier is revived** | Phase 3 found the label was assigned *ex post* by `events.relabel_blowoff`, so no blow-off variance multiplier had reached the GARCH driver since v2. Measured directly here: under the ex-post arm the driver sees **zero** blow-off days on every path. The dynamic criterion (mania drift `g` above a FIT threshold of **0.00984 [0.00590, 0.02080]**, the panel's own blow-off window expressed as a per-day drift over 3,202 run-ups) puts the label on the path the driver produces — 100 % of paths, median 62 blow-off days. Two things are recorded rather than claimed: `volatility.json` carries blow-off at *mania's* value (1.345) where `e3_4/calibration.json` records the diverged knob 9.318, and **Phase 3's own label discloses this** as a documented shortfall of the dead label; and with the label alive the realised blow-off/mania ratio is **0.984** against the panel's **1.289** on the corrected reference, so the multiplier still needs its closed loop re-run. | Report the label as fixed because it now appears in the phase column, without checking that the driver ever counted it | `e4_8/labels.json`; `tests/test_v2_1_phase_4.py::test_blowoff_label_reaches_the_driver` |
| P4-12 | **The post-top reversal leg is re-derived and Phase 3's 1.59 floor is broken** | Phase 3 measured the scripted linear leg as drift-dominated: its realised variance ratio floors at **1.59** (n = 20) whatever the innovation multiplier is set to. An exponential approach to the reversal target over the remaining horizon removes the floor. Searched over half-lives 5-160 d at 400 seeds each: 1.643 / 1.090 / 0.794 / 0.646 / 0.631 / **0.619** / 0.609 / 0.601 / 0.580 / 0.552. The target is the panel's post-top/mania ratio on the **corrected** run-up calm reference, **0.6225**; half-life **50** realises **0.6186**. | Keep the linear leg and report 1.59 as unfixable, or test against the old 1.16 target, whose denominator (E3.3's run-up calm window) sits at a post-crash trough | `e4_8/labels.json` `posttop`; `PREREG_PHASE_4_ADDENDUM.md` section 2.5 |
| P4-13 | The bubble-side multipliers get a **clean reference**, and the old one is shown to be contaminated | E3.3's run-up "calm" window is the 120 days *before* the run-up's start, which sits at a post-crash trough: its pooled sd is **0.0413** against the drawdown family's **0.0217**. E4.1 supplies the corrected window (120 days *after* the start, the quiet beginning of the run-up itself, sd 0.0357), on which the panel's ratios are mania 0.869, blow-off 1.121, post-top 0.541 — i.e. blow-off/mania **1.289** and post-top/mania **0.622**. The mania / blow-off / post-top entries of `volatility.json` are ratios to a median-RV level that describes neither population and are **not comparable** with the crash-side entries. | Continue to test the bubble phases against the old reference, which is how the 1.16 post-top target and the 1.65 blow-off target were stated | `e4_1/episodes4.json` `runup_calm_reference`; `e4_8/labels.json` `panel_targets` |
| P4-14 | The top day is recorded at the **realised** peak, and the off-by-one is measured | `event_meta` now carries `top_day` (the hazard firing), `top_day_realised` (the argmax of the path over the mania run) and their difference. Measured on 300 bull-trap seeds: the offset is non-zero on **89 %** of topped paths, median **-2 days**. The test asserts `top_day_realised` really is the realised maximum rather than trusting the label. | Fix the index and assert it, without measuring how large the error was | `e4_8/labels.json` `topday`; `envs/v2/events.py::realised_top_day` |
| P4-15 | **D6 applied: "Day-N" kept, all three renderings implemented, the ordering factor added to the grid** | The three REG-9 renderings are behind `SyntheticMarketEnv(day_index_mode=...)`: `day_n` (the D6 default, preserving v1 comparability), `none` (the key is *absent*, not rendered as `None`) and `date` (a per-seed random start excluding 1987, 2000-02, 2008-09 and 2020). `simulation/runner_v2.py` had always supported `ordering` but `experiments/arms_v2.py` had no ordering factor, so every LLM cell ever run used `setup_first`; it is a factor now, with a `--orderings` argument and a test. | Change the default before Phase 9's LLM probe has run, which is the evidence that could actually unseat Day-N | `envs/synthetic_market.py`; `experiments/arms_v2.py`; `tests/test_v2_1_phase_4.py` |
| P4-16 | The multi-asset shared event is **specified but not replaced**, and the fitted loading says why it must be | The panel's cross-sectional drawdown share — the share of live names inside a 30 % or deeper drawdown on the same day — has mean **0.236**, p90 0.480 and a 25-year maximum of **0.890** over 417 names. v2's multi-asset extension puts every asset in the same event on the same days, a loading of **1.0**, which the panel never reaches. The replacement (per-asset draws with this common-factor loading) is specified and the value is FIT, but the implementation is **NOT DONE** and the parameter file says so. | Ship the fitted loading as if the mechanism used it | `e4_1/episodes4.json` `cross_section`; `envs/v2/params/events.json` `multi_asset` |
| P4-17 | Every generator change is a switch with v2 behind it, **proved by hash** — *corrected in P4-27* | After each of the five code changes (schedule, events, generator, the day-index renderer, the recorded drift) the 12-path digest under `schedule_mode="v2"` was recomputed and compared with the committed HEAD: `f37972fbce86ee4f2a34ccbd` throughout, including the bull-trap draw ORDER, which the rewrite had to preserve explicitly. With `events.json` applied the digest moves to `19d23eeebcd53984ee693626` and the 95-configuration path hashes show **1,288 non-analyst column changes** — the event block has genuinely changed, which is the point of the phase. | Rewrite the blocks and assert that v2 is preserved without a hash | `path_hashes_phase4_after.json`; `tests/test_v2_1_phase_4.py::test_v2_schedule_path_is_bit_identical` |
| P4-18 | **Kaggle is used for scale, not only for speed, and the guard is re-proved** | The reference row was recomputed on the kernel and agreed with the committed `e2_3/reference_row.json` to a worst relative difference of **0.0** under numpy 2.0.2 / scipy 1.16.3. E4.6 then ran its full seven-arm grid at **1,000** crash and 1,000 bull seeds — double the pre-registered 500 — in 385 s, so the offload bought sample size rather than only wall-clock. Every leakage audit, surrogate and classifier stayed on the reference machine, including E4.5's discrimination, which is a model fit. `datasets/` never left this machine; only derived statistics were shipped. | Offload the audits too, which the reproducibility guard forbids; or keep the pre-registered 500 and bank the time instead of the power | `e4_6/kaggle_guard.json`; `e4_6/kaggle_kernel_summary.json` |

### Phase 4, verification pass (P4-19 … P4-25)

Every quantitative statement in `PHASE_4_REPORT.md` was audited into *measured* and *inferred*, and the
inferred ones tested against stated falsifiers. These entries record what that found; four of the phase's own
claims did not survive.

| # | Decision | Evidence | Alternative rejected | File |
|---|---|---|---|---|
| P4-19 | **Three Phase-4 results were measured before the state they describe existed; two adopted parameters are withdrawn** | A timestamp audit: `events.json` was written at 14:47:39 while `e4_3/hazard.json` (14:18), `e4_5/control.json` (14:41) and `e4_8/labels.json` (14:45) all predate it, and none of those tools pins `schedule_mode` — so all three ran under the **v2** schedule and were quoted as deployed properties. Re-measured on the deployed state at 500 seeds per scenario: the topped share is **0.008 [0.002, 0.016]** against the 0.108 it was adopted on, and the post-top/mania ratio is **0.860 [0.641, 1.169]** against the 0.6186 it was fitted to. The crash block (rise 60.0 [55, 65] against the pinned arm's 58.0 [52, 64]) and the control block (rejection 0.336 vs 0.358, KS 0.408 vs 0.474) **do** hold. The Phase-4 checklist had already contradicted the hazard number (15 % vs 10.8 %) and the report explained it away as "a different seed block" without testing that explanation — an inference presented as a reconciliation. | Leave the parameter file quoting numbers measured elsewhere, on the strength of an untested reconciliation | `e4_9/deployed.json`; `PREREG_PHASE_4_ADDENDUM.md` section 5 |
| P4-20 | **`post_top_drop` and `post_top_len` shipped STIPULATED in Phase 4's own parameter file, and are now FIT** | The hazard is not what fails — it fires on 0.19 of deployed paths. What collapses is the drawdown after the top, because both leg parameters were carried over as v2 DESIGN ranges (U(0.30, 0.50) and U(10, 30)) while the panel holds the data: \|post-drop\| P10/P50/P90 = **0.061 / 0.169 / 0.414** and post-length **8 / 60 / 187** d, n = 3,201 run-ups / 398 stocks. This is a violation of the programme's governing rule inside the phase's own output: the interval was DESIGN where the panel could supply a FIT, because the two were treated as bubble-side parameters rather than episode facts. They are episode facts. Both are now drawn by the inverse-CDF empirical sampler and the half-life re-searched on the deployed state. | Keep the stipulated ranges and re-tune the half-life against them, which is fitting a parameter to compensate for an unfitted one | `e4_11/posttop_recal.json`; `envs/v2/params/events.json` |
| P4-21 | **REG-18's adoption criterion is not evaluable on a 200-day horizon — P4-7's adoption is withdrawn** | The obvious reading (the drop is too shallow) is refuted by the generator's own realised drop, **P50 −0.239, deeper than the panel's median −0.169**. The horizon hypothesis was tested window-matched instead: within every post-top window bin the two populations share, the generator agrees with the panel — 0–50 d **0.0250 [0, 0.063]** vs **0.0294 [0, 0.088]**; 50–100 d **0.0769 [0, 0.192]** vs **0.0357 [0, 0.107]**, CIs overlapping. The panel's headline 0.110 comes from the **3,115** run-ups with a 150–200 day window (0.1124 [0.101, 0.124]), and only **0.9 %** of generated topped paths have even 120 days left (median **29**). The topped share is therefore dominated by remaining horizon rather than by the hazard, and cannot discriminate between mappings. The hazard entry moves ADOPTED → **INCUMBENT**, values unchanged, withdrawal on the entry. Options for making it evaluable are stated (window-matched re-expression; a longer T for bull-trap; an earlier mania start, which is `setup_len` again) and none is Phase 4's to choose. | Re-tune the hazard until the topped share comes back, which would be tuning a generator parameter to a statistic the horizon controls | `e4_12/horizon.json`; `e4_9/deployed.json` |
| P4-22 | **The κ deceleration survives its own falsifier — and my caveat does not** | P4-8 withheld κ = 0 partly because run-ups are defined by a local maximum at the top, which could mechanically flatten the final segment. Tested on four windows: original [start, top] **0.621**, [start, top−21] **0.406**, [start, top−42] **0.358**, and a window whose end is **not selected on price at all** [start, start+504] **0.342 [0.318, 0.368]** (n = 3,186 / 398). Removing the endpoint constraint makes the deceleration **stronger**, not weaker. The measurement stands and the stated reason for withholding the value does not; whether to adopt κ = 0 remains a design decision because it changes what the bull-trap scenario is, but it can no longer be deferred on the selection argument. (Note: `e4_4`'s 0.665 and this 0.621 differ because `e4_4` dropped episodes with a non-positive last-third gain; the unfiltered figure is the one to quote.) | Leave the caveat standing unexamined, which would have parked a fitted value behind an untested objection | `e4_10/verify.json` T1 |
| P4-23 | **Checklist item 10's cause is confirmed; the KS remedy count and the multi-asset loading were wrong** | (i) Item 10: under v2 the partial R² of `crash_discount` is **0.229** with corr(discount, delta drawn) = **1.000**; under v21 it is **−0.000** with corr **0.000** — the inertness is caused by drawing delta from the panel's depth, as stated. (ii) The KS bound's remedy was extrapolated as "about 2,000 seeds"; **measured**, the null floor is 0.211 / 0.145 / **0.103** / 0.075 at n_rej 81 / 162 / 324 / 648, so it needs about **4,000** seeds. (iii) v2's multi-asset loading was stated as 1.0 from reading the code; **measured** on 3-asset paths it is **0.364** (all three in drawdown together on 0.202 of days) against the panel's 0.236 — the direction stands, the number did not. | Publish an extrapolated seed count and a code-read loading as if either had been measured | `e4_10/verify.json` T3, T4, T7 |
| P4-24 | **"Adopting A/B/D closes items 18 and 42" is NOT ESTABLISHED at the achieved n, and the first test of it used an unfounded threshold** | The first version compared the accepted-vs-rejected KS against a stipulated 0.20 and returned "every definition selects on x" — a threshold with no null behind it, which is the failure mode this whole pass exists to catch. Re-run against a null computed at each definition's **own** achieved n_accepted / n_rejected: every definition exceeds its p95, but by **0.0042** for A/B/D against **0.0762** for C, a factor of **18**. At n_rejected = 44 the test has almost no power either way (P4-23: ~4,000 seeds needed). So the claim is **neither confirmed nor refuted**; what is measured is that C's selection is an order of magnitude larger than A/B/D's, and that A/B/D carry no x-band at all, so any residual is rejection on V growth, which the engine makes independent of x. D14's evidence base is unchanged in direction and weaker in strength than section 5.2 claimed. | Report the binary verdict of a test whose threshold was invented, in either direction | `e4_10/verify.json` T6 |
| P4-25 | **The deployed excess is the mix PLUS a named level error, not the mix alone** | P4-1 attributed the residual +15.3 % to the scenario mix. Decomposed under panel phase weights: calm carries **62.2 %** of the variance on 78.4 % of the days, and **post-top carries 12.1 % on 7.2 %** — the largest disproportionate contributor, and exactly the leg P4-20 re-fits. The mix explains most of the gap; the remainder had a specific, correctable cause that attribution had obscured. P4-1's headline finding (the calm level matches, the anchor is kept) is unaffected — that rests on the matched-estimator comparison, which is a measurement. | Leave an attributed residual in the record as though it were explained | `e4_10/verify.json` T5 |

| P4-26 | **P4-1 survives on the deployed state; P4-2 reverses sign there** | E4.0 did not pin `schedule_mode` either (found by the new guard test, not by inspection), so its own numbers were taken under v2. Re-run with the schedule pinned to the deployed v21 at 500 seeds per scenario: **the calm finding survives** — generator 0.022054 [0.021652, 0.022482] against the panel's 0.021672 [0.020860, 0.022439], ratio **1.0176**, inside the CI, so the anchor stays kept and P4-1's headline is confirmed rather than merely defaulted to. **P4-2 reverses**: deterioration was 1.414 (too cold) under v2 and is **2.632 [2.382, 2.938]** deployed against the panel's **1.618 [1.506, 1.746]**, with disjoint intervals — too LOUD. The cause is measured and immediate: the fitted `det_len` has a median of 8 days against v2's 27, so the same `D_V` decline is compressed into a third of the days. `D_V` is still the v2 DESIGN range U(0.10, 0.30) and the panel can supply it; alternatively the `crash_v_drift` tail-share mechanism (implemented, untested) spreads the decline past the deterioration phase. Either way it is a shape question, not a level one. | Report P4-2's v2-state sign as if it described the environment being handed over | `e4_0/phase_levels.{json,md}` re-run with `FP_SCHED=v21` |

| P4-27 | **P4-17's "bit-identical" probe was weak, and the claim needed narrowing** | The 12-path digest used during development matched HEAD under `schedule_mode="v2"` throughout — but once `events.json` exists it still matched, which should not have been possible, because the hazard, the blow-off criterion and the post-top leg are separate switches that events.json also sets. Measured why: `mult["blow-off"]` **equals** `mult["mania"]` exactly, so relabelling mania to blow-off is numerically inert (P4-11's own finding), and of the probe's three bull-trap seeds the only one that tops does so on **day 200**, leaving the post-top leg zero days to run. The probe never exercised the blocks it was supposed to guard. Re-measured on four seeds chosen to top with at least 30 days of post-top window: `schedule_mode="v2"` alone gives `8d8e0107…` while all four switches give `0930d2b8…`, and `0930d2b8…` is **identical to the digest with `events.json` removed entirely**. **Corrected claim: each block has its own switch with the v2 behaviour behind it, and setting all four reproduces v2 exactly; `schedule_mode="v2"` alone reproduces the schedule only.** `tests/test_v2_1_phase_4.py::test_all_four_switches_together_reproduce_v2` locks it, and asserts the probe seeds still top so the test cannot silently become vacuous the way the first one did. | Keep quoting a hash match from a probe that does not exercise the code it certifies | `tests/test_v2_1_phase_4.py` |

### Phase 4, completion pass (P4-28 …)

The items `PHASE_4_REPORT.md` section 7 listed as not done, executed. Each carries the same rule as the rest of
the phase: measured on the deployed state with `schedule_mode` pinned, and no claim that is not tested.

| # | Decision | Evidence | Alternative rejected | File |
|---|---|---|---|---|
| P4-28 | **The rise-time criterion CAN be met, and P4-6's diagnosis is confirmed by direct experiment rather than by conditioning** | P4-6 identified `setup_len` as the binding lever from `corr(onset lag, rise) = 0.684` and from a conditional (rise 32 d on the paths whose onset coincides with the event) — but conditioning on the onset lag **selects on an outcome**, so it could not establish causation. E4.7 sweeps the cause directly, at 200 crash seeds per arm on the deployed state: median setup **58 d → rise 48.0 [42, 52]**; **40 d → 44.0 [39, 51]**; **26 d → 41.0 [38, 45]**; **13 d → 37.0 [34, 41]**, which **overlaps the panel's [29, 35]** and meets the registered criterion. The relationship is monotone across all four arms, so shortening the setup does cause the rise time to fall. Crucially the trade-off REG-9 exists to protect does **not** bind: the day-only macro-phase classifier is **acceptable at every setup range**, with accuracy below its own seed-permutation null p95 in all four (e.g. 0.8425 against 0.8502 at the shortest). **A setup range therefore exists that meets E4.2's criterion and stays at chance on the clock test.** Adopting it is a change to the benchmark's shape — a 13-day median setup leaves far less pre-event baseline — so the value is reported with its consequence rather than written into `events.json` unilaterally. | Leave the rise time recorded as NOT MET on the strength of a conditional that selects on an outcome | `e4_7/calendar.json` `setup_sweep` |
| P4-29 | **`days_since_eps_announcement` does carry an absolute clock, and randomising the quarter grid removes most of it** | REG-9(c) asks for the quarter phase to be randomised per seed; the premise was never measured. Measured here, and the first version of the test was itself invalid: it asked whether the field predicts the raw day index with a null that permuted SEED labels, and returned R² exactly equal to its null (0.0363 vs 0.0363) — because **every path shares the same day axis**, so permuting seeds cannot break a relation that is identical across seeds, which is precisely the relation being tested for. Corrected statistic: accuracy of predicting which third of the quarter the day falls in, from the field alone, pooled across seeds, against a null that shuffles each path's field series **in time**. **v2's fixed grid: 0.8728** against a null of 0.3627 and chance of 0.333 — a large clock. **Randomised per seed: 0.3958** against 0.3625 — 93 % of the excess removed, with a 3.3 pp residual that is still above the null at n = 150. The randomisation is adopted because it strictly dominates; the residual is reported, not explained away. | Implement the randomisation because the register asks for it, without testing whether the clock it removes exists or whether it removes it | `e4_7/calendar.json` `eps_quarter_clock`; `envs/v2/observables.py` |
| P4-30 | **P4-16 was wrong three ways about the multi-asset block, and the fix it proposed does not work** — *numbers superseded by P4-37; the three conclusions stand* | (i) v2's realised common-factor loading is **not 1.0** — measured **0.3998 [0.3666, 0.4329]** [3 assets, 60 seeds]. (ii) The proposed fix, per-asset event draws, **does not change it**: 0.3998 shared against 0.4001 at loading 0.0, a difference of **0.0003**. The reason is measured, not guessed — every asset is in the **same scenario**, so all three crash whether or not they share a schedule, and a ≥ 30 % drawdown lasts long enough that they overlap anyway. (iii) The comparison itself was not population-matched: the panel's 0.2365 is an **all-day** mean over 417 names while the generator's 0.40 is **conditional on a crash scenario**. The discriminating experiment: **flat, with no scripted event at all, gives 0.0531**, and removing the common fundamental factor from crash leaves 0.3829 — so the scripted event drives the co-movement and the common factor does not. The panel's all-day mean sits between the generator's flat and crash arms, and the panel's p90 (0.480) is **above** the generator's crash arm. **The mechanism stays implemented and switchable but is labelled NOT ADOPTED**, because matching the panel would need scenario heterogeneity across assets — a design change to the multi-asset extension, which is Phase 9's. | Ship the per-asset mechanism with a FIT loading as though the loading controlled the statistic it is named after | `e4_15/multiasset.{json,md}`; `e4_10/verify.json` T7 |
| P4-31 | **The blow-off multiplier is calibrated, closing P4-11's outstanding half** | The label reaching the driver was only half the fix: at mania's multiplier 1.3451 the realised blow-off/mania ratio was **1.0160 [0.9375, 1.0965]**, outside the corrected panel reference **1.2895 [1.2146, 1.3713]** (n = 3125 run-ups, 394 stocks). Six closed-loop iterations (1.3451 → 1.6859 → 1.9008 → 2.0064 → 2.0531 → 2.0701) settle at **2.0764**, realising **1.3136 [1.2139, 1.4119]** — inside the panel CI, verified at 1200 seeds. Note the multiplier is not the ratio it produces: 2.0764 buys 1.3136, which is why it had to be solved rather than stipulated. | Leaving the multiplier at mania's value now that the label reaches the driver, i.e. assuming a working switch implies a calibrated one | `e4_13/blowoff_cal.{json,md}`; `params/events.json` |
| P4-32 | **v2's crash V-drift shape is rejected in direction; no magnitude is adopted** | Item 73 asserts the fundamental decline arrives entirely in deterioration (1.0 / 0.0 / 0.0). Against the EDGAR proxy on 270 covered fast-crash episodes (106 with a falling V-hat), the median share in **peak → onset is 0.000 [0.000, 0.000]**, panic 0.024 [0.000, 0.156], stabilisation 0.891 [0.631, 1.000]. The direction is robust — the share before onset is **0.000 at every de-lag** — but the magnitude is **not identified**, moving over 0.508–0.891 across the de-lag sensitivity because EDGAR filings are dated by filing rather than by the period they report. Closes **TESTED and REJECTED in direction, magnitude left open**; the mechanism ships implemented, switchable and unadopted. | Adopting a fitted tail share from this proxy, which would import the reporting lag as if it were the phenomenon | `e4_14/crash_v.{json,md}`, `episodes_v.csv` |
| P4-33 | **The event redesign did not collapse discrimination, but it did weaken the two event scenarios modestly** | Re-run on the deployed state beside Phases 3 and 2. In all four scenarios both gaps stay strictly positive with CIs excluding zero, so L5 still sits between the mandate oracle and the best trivial policy — no collapse. But L5's advantage over the best trivial policy narrows **0.0703 → 0.0625 in crash** and **0.0749 → 0.0688 in bull-trap**, with best-L5 loss rising in both; coverage moves in opposite directions (crash 0.585 → 0.553, bull-trap 0.637 → 0.646). `flat` and `sustained_bull` are unchanged to three decimals, as they must be — no scripted event. Which of the five changes caused the weakening is **not isolated by any experiment here** and is reported rather than explained. | Reporting "no collapse" as "no effect", when two of the four scenarios moved against the benchmark | `e4_16/discrimination.{json,md}`, `l5_phase4_after.csv` |
| P4-34 | **The L2b selectivity narrowed, and the obvious reading of that is FALSE** | The SEP level-free audit on the deployed state (1600 paths, 288 000 modelled rows) gives selectivity **0.126792 → 0.107427**, a 1.94 pp narrowing — still a **FAIL** against the 10 pp margin. It is **not** the non-price fields leaking less: **full-field accuracy did not move at all** (0.782507 → 0.782618, +0.01 pp on n = 288 000). The entire narrowing is the price-only baseline **rising** 1.95 pp (0.655715 → 0.675191), with the day-only classifier rising 2.13 pp alongside it (0.489000 → 0.510347). Selectivity is a difference and only its subtrahend moved, so the gap closed from the wrong side: macro phase became *more* readable from price alone. | Reporting the narrowing as progress on field leakage — the reading the headline number invites and the components refute | `e4_16/audit_after_levelfree.{md,pkl}`; `tools/phase4/e4_17_sep_audit.py` |
| P4-35 | **Phase-group R² is not comparable across the two hand-overs; only `all` and `calm` are population-matched** | The fitted schedule changed the panel's composition: deterioration 22 157 → 7 902 rows, mania 39 225 → 22 399, blow-off 19 418 → 33 048, stabilisation 53 585 → 66 667, post-top 3 139 → 6 325. So the audit's `event` group loses 14.7 % of its rows and `resolution` gains 28.7 %, and their R² cannot be differenced across states. `all` is exactly matched (288 000 both) and `calm` is matched to 0.14 % (119 419 → 119 588). On the matched groups: `all` full-field **0.8432 [0.8355, 0.8508] → 0.8211 [0.8108, 0.8303]** (CIs disjoint, a real fall of 0.022); `calm` full-field **0.2125 [0.1414, 0.2695] → 0.2260 [0.1621, 0.2816]** (CIs overlap, **no detected change**). The shuffled-V control sits at zero in both (−0.0016, −0.0015). | Differencing the `event` and `resolution` rows, which look like the most relevant groups and are the two the redesign made incomparable | `e4_16/audit_after_levelfree_L2.csv`, `_L4.csv` |
| P4-36 | **The event redesign did NOT narrow the calm level-free channel — the brief's own question, answered negatively** | The brief's quantity is the **calm-TRAINED** level-free R2(x), not the audit's calm row: `leakage_audit.py` fits across all phases and masks afterwards (lines 297-300), so its calm row is cross-phase-trained and is a train/eval regime artefact. Measured with `tools/phase3/e3_9_calm_trained.py`'s estimator imported **unchanged**: **+0.3493 [+0.3218, +0.3743] → +0.3594 [+0.3282, +0.3859]**, delta **+0.0101 with CIs overlapping almost completely** — no detected change, and the point estimate moves the wrong way. Full-field: 0.5501 → 0.5435, also overlapping. Sign accuracy flat (0.814 → 0.812). Phase 3 was **re-run in the same process** and reproduced the stored file on **all six cells**, so this is not estimator drift. With E3.8's decomposition unchanged (0.203, and unchangeable by construction — its arms have no event block), the events-and-sentiment share goes **0.146 → 0.156**. A real negative result: the redesign changed which events happen and when, and none of it touched what a calm-day reader recovers from the level-free fields. That points Phase 6 at the fields and the sentiment feedback — Phase 5's remit — not at the schedule. | Quoting E4.17's calm row (0.2125 → 0.2260) as the answer, which measures a different estimand and would have been reported as "no change" for the wrong reason | `e4_18/calm_trained_phase4.{json,md}`; `tools/phase4/e4_18_calm_trained.py` |
| P4-37 | **A cited evidence file did not exist, and the numbers it was supposed to hold were wrong** | `PHASE_4_REPORT` section 9.3 and P4-30 cited `e4_15/multiasset.{json,md}` for the multi-asset result. **The directory was never created** — the figures came from the tool's stdout. Found while compiling the file list for `PHASE_4_CHANGED_FILES.md`, not by any test. Re-running the tool produced the file and **different numbers**: crash 0.352 → **0.3998 [0.3666, 0.4329]**, flat 0.042 → **0.0531**, no-common-factor 0.361 → **0.3829**, per-asset gap 0.006 → **0.0003**. The tool is **deterministic** (two runs agree on every arm to all digits) and nothing it depends on changed (every `envs/` file predates its mtime; the 17:07 `events.json` rewrite leaves crash prices bit-identical, tested directly for `blowoff_mult`, `post_top_half_life` and `blowoff_mode`), so the earlier figures cannot be reproduced and their provenance is **unknown**. All three of P4-30's conclusions survive on the corrected numbers; only the magnitudes move. **The method lesson, and it is the sibling of P4-19's:** a result quoted from stdout has no provenance. A phase should verify that every file it cites exists before the citation is written — this one was caught by accident. | Leaving the section as written, or quietly swapping the numbers without recording that the citation was empty | `e4_15/multiasset.{json,md}` (now on disk) |
| P4-38 | **Nine of the twenty-two test files had never been run, and six tests were failing — four since the main pass** | Section 7's "remaining suites" row named five files; eleven of twenty-two had been run and **nine had not**. Running them: **6 failed, 57 passed, 1 skipped**. Two failures were caused by this pass's own `events.json` label fix and were resolved by **re-freezing deliberately** (25 files, `3473b174e5172707…` → `869e4fbf4b6e64ba…`; only label strings changed, values asserted byte-identical). Of the other four, **three were the P4-19 ordering defect inside the test suite**: `test_schedule_ranges` and `test_crash_criterion_and_delta_matters` call the generator **without pinning `schedule_mode`**, so they asserted v2's constants against the v2.1 state. Measured: pinned `v2` gives 0/200 range violations vs 158/200 unpinned (`det_len` 3–26 against v2's 15–40), and δ moves the drawdown under `v2` (−0.6347 vs −0.5226) but is **inert to full float precision** under `v21` (−0.5299021470933106 both), confirming item 10's documented inertness. Both now pin `v2` and each gained a companion test asserting the v2.1 behaviour against the fitted grids read from `events.json`. The fourth, `test_known_defect_registry_matches_strict_xfails`, was a Phase-4 bookkeeping miss: Phase 4 added a strict xfail to its own suite and never registered it. **Lesson, sibling to P4-19 and P4-37: a phase that changes the generator must run the WHOLE test tree — its own suite tests its own changes, and what breaks is the older tests encoding the behaviour it replaced.** | Reporting "the remaining suites pass" on the five files section 7 happened to name, which would have left four Phase-4-caused failures undetected | `tests/test_v2_generator.py`, `tests/test_v2_1_phase_0.py`, `tests/known_defects.py`, `tests/v2_freeze_manifest.json` |
| P4-39 | **The hazard has two parameter files that disagree, and the status word `INCUMBENT` does not describe what is deployed — a question for the team** | `envs/v2/params/hazard.json` holds v2's (h0 3e-4, b 6.0); `events.json`'s `hazard` block holds E4.3's (h0 6.712e-4, b 5.419). `generator.HAZARD_H0` reports the former, `GenConfig()` defaults to the latter — both deliberate, since `test_v2_1_phase_4.py`'s digest probe builds its v2 baseline from the module constants. The inconsistency is the **label**: the entry is marked `INCUMBENT`, whose `_status_key` reads "the v2 behaviour stays", while the value in force is E4.3's. The entry's own `adoption_withdrawn` field says what was meant — "The values below stay in force as the INCUMBENT and the criterion needs re-expressing" — so the value is deliberate and the vocabulary simply lacks a term for "in force, adoption withdrawn, pending a re-expressed criterion". **No parameter was changed**: reverting to v2's hazard would move a measured outcome (v2's incumbent gives topped share 0.064, outside the panel CI, per `outcome_not_target`). `test_hazard_loader_is_loud` now asserts the override chain explicitly so neither file can drift unnoticed. The team owns the status vocabulary and REG-18's re-expression. | Silently reverting the hazard to v2's values to satisfy the status word, which would change the generator on a labelling technicality | `envs/v2/params/{hazard,events}.json`; `tests/test_v2_1_phase_0.py` |

### Phase 4, acting on the section 5.1 recommendations (P4-38 … )

The team authorised acting on the seven recommendations, under the standing rule that nothing may rest on
inference. Each was tested before it was acted on; **two of the recommendations were refuted by their own
tests** and are recorded as such rather than quietly dropped.

| # | Decision | Evidence | Alternative rejected | File |
|---|---|---|---|---|
| P4-39b | **The status vocabulary is now enforced by the loader, and it was already broken** | Item 7 recommended adding a term. Measured first: **2 of 10 entries carried a status `_status_key` does not declare** (`crash_v_drift`, `multi_asset`), and `hazard` carried `INCUMBENT`, whose declared meaning is "the v2 behaviour stays", while E4.3's FIT value was in force. Nothing checked any of this -- `events_params.load` validated provenance and intervals but never the status word, which is why it drifted. The loader now **raises** on an undeclared status; the guard was confirmed to fire on the live file before the file was fixed. Three terms added (`IN FORCE, ADOPTION WITHDRAWN`, `TESTED, REJECTED`, `TESTED, NOT ADOPTED`) and three entries reassigned. **No value changed** -- asserted byte-identical. | Adding the vocabulary term as documentation only, which is exactly what had already failed | `envs/v2/events_params.py`; `envs/v2/params/events.json`; `tests/test_v2_1_phase_4.py::test_status_vocabulary_is_declared_and_enforced` |
| P4-40 | **Item 3 adopted: the crash depth draw is centred on the arm factor, and `crash_discount` is a live factor again** | E4.2 drew depth unconditionally, discarding `delta`; the 0.55-vs-0.85 drawdown spread was **0.0000 to full float precision**. `depth_mode="centred"` shifts the fitted distribution so its centre tracks delta. Measured: spread **0.0000 → −0.0814** (73 % of v2's −0.1121); **bit-identical** to the unconditional draw at the reference δ = 0.70, so the deployed state is unchanged; **IQR 0.1333 either way**, so the fitted shape is preserved -- it is a pure location shift, not a re-fit. Clipping quantified rather than assumed: 0 % of draws hit the ceiling at δ ≤ 0.70, 21.7 % at δ = 0.85, 61 % at δ = 0.95. | Re-expressing checklist item 10 to accept the loss, which was the alternative REG-8 anticipated and which discards a controlled factor rather than repairing it | `envs/v2/schedule.py`; `e4_20/depth_cal.{json,md}`; `tests/test_v2_generator.py::test_delta_moves_the_drawdown_under_the_centred_depth_draw` |
| P4-41 | **The crash depth has a STRUCTURAL floor near −0.46, so no dynamics formulation can meet D5's coverage criterion -- `depth_gain` is NOT adopted** | E4.19 found depth-only failure dominates the coverage shortfall in all seven arms (4–7× duration-only). E4.20 ran a closed loop on it and **the loop does not close**: realised depth saturates at −0.4510 (gain 0.45 and gain 0.23 give the same value), and the best available gain reaches −0.4614 [−0.4695, −0.4550] against the panel's **−0.3693**, verified on 1200 disjoint seeds. Three causes tested: **not** rejection sampling (rejection share **0.000 at every gain**); decomposition gives **Δlog V ≈ −0.34** from `D_V`, a **stipulated** uniform [0.10, 0.30] labelled DESIGN, plus **Δx saturating at −0.279** from the panic-phase mispricing excursion (Phase 3, frozen). Sweeping `D_V` to [0.00, 0.06] still leaves −0.3805, and the box share peaks at 0.752 then falls. **The generator cannot produce a panel-median crash**, and closing the gap needs `D_V` and Phase 3's panic multiplier to move together -- a cross-phase calibration outside Phase 4's remit. | Adopting gain 0.2078: it scales the fitted depth distribution to a fifth of its fitted value, hollowing out E4.2's central achievement, and still misses the target | `e4_20/depth_cal.{json,md}`; `e4_19/criteria.{json,md}` |
| P4-42 | **Two of section 5.1's own claims were REFUTED by testing them, and the D5 recommendation was rewritten** | (i) I claimed the script-share metric has a noise floor that makes B and C indistinguishable from unscripted. **Refuted** -- with 2000-resample intervals every arm separates from the unscripted arm D. (ii) I claimed item 6's root cause was targets adopted without checking the generator could express them. **Refuted for coverage** -- `dd30_fast` is already restricted to episodes that fit a 200-day horizon, so the box was window-matched before Phase 4 touched it; the 0.70 threshold exceeds the panel's 0.6433 [0.6074, 0.6791] because a P10–P90 box on two correlated quantities cannot capture 0.80. (iii) I claimed B and C were tied. **Refuted** -- B beats C on coverage, +0.0594 [+0.0115, +0.1068]. What the test DID find is worse than what I alleged: **C, fully scripted with no feedback, scores significantly BELOW the arm with no script at all** (−0.0162 [−0.0185, −0.0140]), so the ranking metric is **not monotone in scriptedness** and cannot be used at all. The conclusion (adopt B) survives on corrected evidence -- D eliminated, A at λ 0.25 dominated (+0.0410 [−0.0130, +0.0903], not separable, at the highest script share) -- but the reasoning that produced it did not. | Letting the original recommendation stand because its conclusion happened to survive | `e4_19/criteria.{json,md}`; report sections 5.1, 9.12 |
| P4-43 | **Item 2 adopted: control definition A, closing weakness items 18 and 42** | D14 taken on evidence re-measured on the DEPLOYED state (800 seeds, rejection off, `schedule_mode` pinned), with each definition tested against **its own** null — the definitions reject at different rates and the KS null floor depends on n, so a shared threshold would have been the wrong test. **Definition C: rejection 0.330, KS(daily sd, accepted vs rejected) 0.4404 against a null floor of 0.1024** — it selects on the hidden state at 4.3× its own null. **Definition A: rejection 0.150, KS 0.1098 against a null floor of 0.1353** — *below* its own null, no detectable selection. A control that selects on x leaks x, which is what the benchmark exists to hide. A is the simplest of the three non-selecting definitions and carries no x-band. **Both `test_sustained_bull_selection` tests XPASS(strict) under A**, which is the signal the defect is closed; the xfail markers and both registry entries are removed. The FIT v-threshold 1.3233 stays recorded and unapplied — changing it now would move the rejection rate at the same time as the definition, confounding both. | Keeping C for the scenario clock and its 32.9 % rejection, which is a convenience argument against a validity one | `envs/v2/params/events.json`; `tests/known_defects.py`; `tests/test_v2_1_{stats,phase_4}.py` |
| P4-44 | **Item 4 REFUTED by testing it: κ = 0 cannot be adopted alone — it makes the bull trap 96 % rejected** | Section 5.1 recommended adopting κ = 0 because the panel's run-ups decelerate and the endpoint-selection caveat had been refuted. That evidence stands and is not withdrawn. But the *consequence* had never been measured, and it is disqualifying: `check_validity` requires `x.max() >= 0.30` for a bull trap, and at κ = 0 the median max-x is **0.1555**, giving a **rejection rate of 0.963** (600 seeds, rejection off, schedule pinned) against 0.108 at the incumbent κ = 0.04. The surviving 4 % would be a heavily selected tail — the same selection defect P4-43 has just removed from the control. Sweep: κ 0.00/0.01/0.02/0.03/0.04 → rejection 0.963/0.903/0.325/0.153/0.108, median max-x 0.156/0.211/0.352/0.404/0.421. κ = 0 needs the bull-trap criterion — itself a **stipulated** threshold hardcoded in `check_validity` — re-expressed at the same time. Joint decision, now costed. Status set to TESTED, NOT ADOPTED; **no value changed**. | Adopting κ = 0 on the deceleration evidence alone, which would have left the bull-trap scenario ungenerable and selected | `envs/v2/params/events.json` `mania_drift`; the sweep above |
| P4-45 | **Adopting D14 reduced the field leakage — and invalidated two audits, through a sequencing error of mine** | Definition A changes **60/60** sustained-bull paths (flat/crash/bull-trap bit-identical, as the criterion is sustained-bull-only), and every sustained-bull row carries the `calm` macro label — 40 000 rows, ≈ 27 % of the calm population — so sections 9.5 and 9.9 ceased to describe the deployed generator the moment A was adopted. Re-measured on the post-D14 state (1600 paths, 288 000 modelled rows, `shown_fields` identical so the estimator is unchanged): **full-field macro accuracy 0.782618 → 0.760160 (−2.25 pp)**, **worst-group selectivity R²(x) 0.687764 → 0.482106 (−0.206)**, L2b selectivity 0.107427 → 0.103524 (still FAIL against the 10 pp margin), calm best R²(x) +0.2260 → +0.2326 (unmoved), shuffled-V at zero in both. **This is the opposite of P4-34's pattern and the direction the benchmark wants**: there the gap closed because the price-only baseline rose, here the field channel itself fell. **The method defect is mine and is P4-19's, applied to sequencing rather than to a tool**: the tools pinned their configuration correctly, but I ran a 77-minute audit and a 40-minute comparison and only then adopted the parameter change that invalidated both. Measure after the parameter file settles — the phase had written that lesson and I applied it to the tools without applying it to the order of work. Pre-D14 state preserved in `_panels/sep_phase4_preD14.pkl` and `e4_16_preD14/` rather than overwritten, because the before/after is itself a result. | Overwriting the pre-D14 audit, which would have destroyed the only measurement of what adopting A did | `e4_21/audit_after_levelfree.{md,pkl}` against `e4_16_preD14/`; report section 9.14 |
| P4-46 | **D14 also repaired the sustained-bull scenario, which was nearly unscoreable — an effect nobody predicted** | Re-running the discrimination table post-D14 (same design, pre-D14 table preserved in `e4_16_preD14/`): flat, crash and bull-trap are unchanged to three decimals, as they must be since the control criterion is sustained-bull-only; **sustained-bull coverage rises 0.044 → 0.374, an 8.5× increase, and the share of runs with NO resolvable step falls from 10 % to zero**. Mechanism measured, not inferred: definition C's x-band clamps mispricing to [−0.10, 0.15], holding median \|x\| at **0.0143** with only **3.5 %** of steps resolvable at θ = 0.05; under A the median is **0.0362** and **35.6 %** are resolvable. The best L5 policy in that scenario also moves from `price_only` to `level_free`, matching the rest of the table. In all four scenarios both gaps stay strictly positive with CIs excluding zero. **D14 was framed as a leakage question and it was also a usability one**: a quarter of the benchmark's cells had coverage 0.044 and one run in ten produced nothing to score. Not predicted — found by re-running the table after the adoption, which is the practice P4-45 exists to enforce. | Reporting only the leakage half (P4-43, P4-45) and never re-running the discrimination table, which would have left an 8.5× coverage gain unrecorded | `e4_21/discrimination.{json,md}`, `l5_phase4_postD14.csv`; report section 9.15 |
| P4-47 | **The report's parameter table is now CHECKED against the deployed file, not merely generated from it** | The section-4 table was written by hand, regenerated from `events.json` after the verification pass, and then went stale **again** across two later adoptions — it read `control \| INCUMBENT \| C — D14 open` while definition A was in force, so the report's own parameter summary described the wrong deployed state. Generating it was an intention; this makes it a check. `test_phase4_report_parameter_table_matches_events_json` parses the table and asserts every entry's status equals the file's, and `test_phase4_status_words_are_declared_in_the_report_too` duplicates the loader's vocabulary guard at the documentation layer, because the two failed together. **Verified by negative control**: re-injecting the exact staleness that was present makes the test fail with `[('control', 'ADOPTED', 'INCUMBENT')]`, and restoring it passes. This closes the phase's recurring defect class (P4-19, P4-37, P4-38, P4-45) at the one place it kept recurring — a number whose provenance is assumed rather than re-checked after a parameter moves. | Leaving the table generated-but-unchecked, which is exactly the state it was in when it went stale twice | `tests/test_docs_numbers.py` |

## Phase 5 — observables (v2.1)

| # | Decision | Evidence | Alternative rejected | File |
|---|---|---|---|---|
| P5-1 | **The SF Fed daily index is smoothed at source, so the "AR(1) of daily sentiment" is fitted on a DECONVOLVED series under the documented kernel, and design A's parameters are 200-day WINDOW fits** | Before any fit: AC(1) = 0.9967 and sd(Δs)/sd(s) = 0.081 ≈ √(2(1−λ)) at λ ≈ 0.9967 — the signature of an exponentially weighted trailing average of a persistent series. The FRBSF Economic Letter 2020-08 (read 6 Sep 2026) states the construction ("a trailing weighted average … weights that decline geometrically … a depreciation rate of 5%"). Deconvolved at λ = 0.95 (LIT): the day-level series has sd 0.338, AC(1) 0.349 [0.286, 0.407] and a plateau ≈ 0.30 out to lag 21; the 200-trading-day window fits (57 windows) give ρ **0.211 [0.155, 0.256]**, b₀ +0.011, b₁ **+0.111 [0.062, 0.132]** per sd, residual sd 0.972 — like-for-like with the 200-day benchmark path, which is also the population rule (i)'s references are computed on. | Fitting the AR(1) on the published series (ρ = 0.9967, a smoothing artefact); or the full-sample AR form (ρ 0.40), which keeps the slow component a 200-day path cannot carry | `e5_5/sentiment.{json,md}`; `PREREG_PHASE_5.md` section 2 |
| P5-2 | **The Phase-5 switches are inert when off — proved against a HEAD worktree, because the inherited path-hash fixture was stale** | Function level: every block reproduces the committed HEAD functions on 50/50 random inputs. Path level: the fixture run on the patched code (no `observables.json`) against `path_hashes_phase4_after.json` showed **722 changed columns**, all in the 10 sustained-bull configs (D14 changed every sustained-bull path) and the 20 crash configs at δ ≠ 0.70 (the centred depth draw is bit-identical only at 0.70) — the fixture (5 Sep 17:13) predates the final `events.json` (6 Sep 03:08). Re-run from a `git worktree` at HEAD: **0 non-analyst column changes** across all 95 configurations. | Quoting the 722 as a Phase-5 change, or "fixing" the fixture by regenerating it on the patched code (which would prove nothing) | `path_hashes_phase5_before.json`, `path_hashes_phase5_v2check.json`, `path_hashes_phase5_before.log`; `tests/test_v2_1_phase_5.py::test_observables_switches_preserve_v2` |
| P5-3 | **Two pre-registration corrections made before the outputs they concern were read, and one inherited citation slip caught by the verify stage** | (i) Design B's net within-stock variance subtracts the trailing-sum noise s_EPS²/4, not 2·s_EPS² (a seasonal difference's variance, which does not enter the level of log P/E); corrected before E5.1/E5.2 output was read. (ii) The inherited table cited gbt's −0.394 as the "best" cross-phase-trained level-free calm R²; `prereg_power.py`'s verify stage found the file's best is ridge's −0.249 — the file's value stands, no threshold depends on it. All other 28 inherited figures reproduce; the stored post-D14 panel is bit-identical to the current generator on eight regenerated paths. | Leaving the slip in the table (the P4-37 failure mode: a cited number that is not the file's) | `e5_0/power.{json,md}`; `PREREG_PHASE_5.md` sections 1 and 4.2 |
| P5-4 | **Every registered threshold is decidable at its stated n (PP1–PP6), including the permutation null E5.4's admissibility rests on** | PP1: the KS null floor at 1,600 vs 50,000 is 0.034; pass rate 1.00 under D = 0 and 0.00 at a true D = 0.10. PP2: the onset ΔAUC null p95 is +0.017 at 200 seeds (E3.5's measured +0.032 is inside the scale). PP5: the real estimator on a synthetic SEP-sized panel gives a permutation null of order 10⁻⁵–10⁻⁴ for a 6-column block, against an expected analyst effect ≥ 0.014. | Registering the rules without the nulls (five phases in a row lost a re-run to an undecidable criterion) | `e5_0/power.{json,md}` |
| P5-5 | **E5.6's run-up turnover ratio is BELOW 1 — the register's premise is not reproduced on this panel** | On E4.1's 3,202 run-ups (every start/top index re-checked against the stored run-up value, 0 mismatches; 3,019 usable): median log ratio **−0.015 [−0.032, −0.001]**, ratio 0.985 [0.969, 0.999]; the level-free loading β_ru = −0.012 [−0.020, −0.006] per unit positive trailing return, with sub-period signs +0.10 / −0.06 / −0.08 / −0.06. The registered rule's first clause ("the ratio's CI excludes 1") is satisfied in letter and contradicted in substance (GSY's elevated turnover was the premise). Design A's fits: ρ_v 0.526 [0.522, 0.529], β 0.203 [0.198, 0.206], σ_e 0.347 [0.339, 0.351]. The decision waits for the arm's onset audit (rule clause 2) and is recorded in P5-9 with the interpretation stated. | Adopting B on the letter of clause 1 alone, which would write a sign-unstable −1.2 % loading into the generator as a "run-up feature" | `e5_6/volume.{json,md}` |
| P5-6 | **E5.7(c)'s registered reference set was too narrow: the technical fields are price-derived and join the reference side; the rule applies to the non-price fields** | The baseline onset audit (200 + 200 seeds, 500 circular shifts) failed eleven (field, transition) pairs as registered, seven of them `SMA20` / `SMA50` / `MACD` / `MACD_signal` — deterministic functions of the price path, e.g. \|Δ log SMA50\| at panic→stabilisation AUC 0.755 against the five-score reference's 0.655. REV-11b's purpose is that a field may not detect a transition better than the price path does; a subset of the price path's own scores is not that reference. Corrected before any arm's onset table was read (`PREREG_PHASE_5_ADDENDUM.md` §1, with the disclosure); both verdicts are recorded in every onset result. Under the corrected rule the baseline fails on exactly the v2 analyst field (crash calm→deterioration, ΔAUC +0.034 vs null +0.001) and the v2 sentiment fields (bull-trap calm→mania, +0.017 to +0.023 vs +0.010) — the two hidden-state channels the phase exists to close. | Reporting the technicals as leaks, or dropping the failing rows quietly | `e5_7c/baseline/onset.{json,md}`; `tools/phase5/e5_7c_onset.py` |
| P5-7 | **E5.5 rule (i): designs A, B-full and B-half meet the data's 200-day window statistics; C does not, on the weekly correlation, by 0.011** | Generator medians over 1,600 paths (path-cluster CIs) against the deconvolved SF Fed 200-day windows (ACF(1) 0.200 [0.152, 0.258]; corr 0.006 [−0.028, 0.019]): A 0.205 [0.201, 0.209] / +0.017 [+0.013, +0.021]; B-full 0.216 / +0.004; B-half 0.207 / +0.011 — all overlapping. C is a weekly field and is judged on the AAII 40-week references (ACF(1) 0.537 [0.478, 0.581]; corr 0.242 [0.142, 0.311]): 0.570 / **+0.322** — the correlation sits 0.011 above the reference's upper limit because the window-median loading (b₀ 0.298 per sd) overshoots the window-median correlation (the ratio of medians is not the median of the ratio). The rule is not relaxed to admit C (`PREREG_PHASE_5_ADDENDUM.md` §3); rule (ii) decides among A, B-full and B-half. | Admitting C on the size of the miss, which is a moved threshold | `e5_arms/stats.json`; `e5_5/sentiment.json` |
| P5-8 | **The 3.3 pp residual clock in `days_since_eps_announcement` (P4-29) is the window's EDGE plus single-split sampling noise — resolved, no redesign** | E4.7's `eps_clock` re-run unchanged on 200 flat seeds with the randomised quarter grid, under v2's lags and under the FIT 8-K grid, on the registered split and five more: interior days 64–137 give **−1.56 ± 1.18 pp** (v2 lags) and **+0.09 ± 1.12 pp** (FIT lags) over the null — chance; the full window gives +0.78 ± 1.23 and +1.47 ± 1.63 pp — within one split-sd of zero. The registered single split read +2.0 / +3.3 pp full-window and −0.1 / +1.5 pp interior, i.e. the same numbers before their sampling variability was measured. The edge mechanism: the first days of a path can only be reached from an announcement in the burn-in, which fixes those days' quarter position across seeds. | Redesigning the field, or reporting the single-split +1.5 pp interior excess under the FIT lags as a residual leak | `e5_2/clock/clock.{json,md}` |
| P5-9 | **The first per-field-group attribution: VOLUME, not the analyst field or P/E, is the largest single carrier of the hidden state in the fields handed over** | E5.7(a) baseline on the stored post-D14 panel (1,600 paths, 288,000 modelled rows, the audit's gbt, paired 500-resample cluster bootstrap): add-one ΔR²(x) over the level-free base on all rows **VOL +0.220 [+0.204, +0.236]**, **VAL +0.133 [+0.114, +0.155]**, SENT +0.062 [+0.055, +0.068], LEVELS +0.007 (n.s.), ANALYST +0.002 [−0.001, +0.006], IV +0.002; drop-one from the full set VOL +0.085, ANALYST +0.055, LEVELS +0.052, VAL +0.034. Calm-trained: base **0.3213** (= E4.21's calm-trained level-free figure, the same code path), VOL +0.052, SENT +0.027, VAL +0.021, ANALYST +0.020. Macro-phase clock: VOL **+6.6 pp** of the 10.7 pp full-minus-base gap, VAL +3.1, SENT +1.2. The volume field's `1.2·\|x_{t−j}\|` loading — a term with no read source (weakness 24) — is what E5.6 removes; the analyst field's information is mostly redundant with the valuation group's. | Ranking the fields by the reviews' suspicion (analyst, P/E, sentiment) rather than by measurement | `e5_7a/baseline/ablation.{json,md}` |
| P5-10 | **E5.1: design B — the FIT wandering multiple (a between-stock draw from the EDGAR P10–P90 grid plus a within-stock log-AR(1) at ρ_d 0.9966 with sd net of x and the EPS noise) — is adopted at width P10–P90 by the rule as registered** | KS under the addendum §5 convention: A 0.095 (vs the data truncated to the width), B 0.082 (vs the untruncated cross-section), both < 0.10. ΔR²_add(VAL) (x, P-all, 1,600 paths, paired 500-resample cluster bootstrap): A **+0.0254 [+0.0134, +0.0367]**, B **+0.0114 [+0.0027, +0.0204]**; paired A − B **+0.0139 [+0.0014, +0.0256]** — above zero, so the rule gives B. The v2 baseline's VAL was +0.133: either FIT design removes most of the P/E channel. Calm-trained co-statistic the other way (A +0.020, B +0.039); the inversion share is 0.4163 on both (price itself). | A as the simpler design (it loses the registered comparison); deciding on the calm co-statistic, which the register did not make primary | `e5_arms/multiple_{A,B}_P10-P90/ablation.{json,md}`; `e5_arms/decisions.md` |
| P5-11 | **E5.6: design A adopted under the addendum §2 reading of clause 1 (the CI must exclude 1 in the premise's direction); the clause's letter would give B, and both verdicts are recorded** | The volume channel collapses from +0.220 (v2) to A **+0.0042 [+0.0026, +0.0059]** and B **+0.0036 [+0.0021, +0.0051]** — the `1.2·\|x\|` loading was the channel; paired A − B +0.0006 [+0.0002, +0.0011]. Clause 1: the run-up log-ratio CI [−0.032, −0.001] excludes zero, from below (P5-5). Clause 2: B's `volume` / `volume_ratio` pass the onset rule at every transition (worst excess −0.017 vs null p95 +0.011). | B on the letter of clause 1 — a sign-unstable −1.2 % loading written into the generator for 0.0006 of R² | `e5_arms/volume_{A,B}/ablation.{json,md}`; `e5_7c/volume_B/onset.json`; `e5_arms/decisions.md` |
| P5-12 | **A tool regression found and repaired before any number was read from it: the arm ablations' table builder required a FULL fit that the arm runs no longer make** | Making the arm runs skip FULL (needed only for drop-one) left the table builder's guard (`BASE and FULL both present`) producing empty tables for four finished arms; `e5_arms.py`'s skip rule (`"tables" in the file`) would have treated them as done and `e5_decide.py` reported them as pending. Fixed: FULL optional in the tables and the markdown; the four arms' tables recomputed from their stored fits (12 s each, no refit); the repair checks emptiness, not presence. | Leaving the arms as they were (the contests would have stayed pending) | `tools/phase5/e5_7a_ablation.py` |
| P5-13 | **E5.5: rule (ii) selects design A (returns only) as the sentiment default; both B variants are the labelled sensitivities; D15 goes to the team with the table** | Rule (i) met by A, B-full, B-half (daily ACF(1) 0.205–0.216 vs 0.200 [0.154, 0.265]; corr 0.004–0.017 vs 0.006 [−0.028, 0.019]); C fails by 0.011 (P5-7). ΔR²_add(SENT) (x, P-all, seed-paired): v2 +0.062; **A −0.0007 [−0.0012, −0.0002]**; B-half +0.0205 [+0.0168, +0.0236]; B-full +0.0813 [+0.0731, +0.0883]; C −0.0009. B's link is retained only if its paired increment over A includes zero — B-half +0.0211 [+0.0176, +0.0242] and B-full +0.0819 [+0.0740, +0.0890] do not. All three pass the onset rule (mania onset ΔAUC −0.005 to −0.007 vs null +0.007). The FIT valuation link puts *more* x into the field than v2's tanh(2x) did. | Adopting B on the grounds that its link is FIT (the rule makes the increment, not the provenance, decide); relaxing rule (i) to admit C | `e5_arms/sentiment_*/ablation.{json,md}`; `e5_arms/stats.json`; `e5_arms/decisions.md` |
| P5-14 | **D10 has a measured cost: rendering the dividend yield carries +0.038 of R²(x) under a constant multiple** | `epsdiv_v21_shown` vs `epsdiv_v21_hidden` (the FIT EPS and DPS chains under the v2 multiple; same hidden paths): ΔR²_add(VAL) **+0.0897 [+0.0747, +0.1045] shown vs +0.0515 [+0.0399, +0.0628] hidden**, paired +0.0382 [+0.0292, +0.0480]. The yield is 4·DPS/P with DPS ∝ τ·V/k smoothed: a valuation ratio carrying e^{−x}/k. The VAL fields pass the onset rule at every transition. The decision stays the team's (both renderings carried, default `shown`); the final ablation measures the group under the wandering multiple. | Deciding D10 in the phase (the plan reserves it); or reading the cost as a leak to be closed rather than a rendering choice with a price | `e5_arms/epsdiv_v21_{shown,hidden}/ablation.{json,md}`; `e5_7c/epsdiv_v21_shown/onset.json` |
| P5-15 | **The sentiment feedback (`b_pred`) is worth about 0.03 of the calm level-free residual, a quarter of what E4.21 left to "fields and feedback"; the rest is the process stack's** | The `b_pred = 0` arm (v2 sentiment, feedback off, re-simulated): calm-trained level-free base **0.3213 [0.291, 0.350] → 0.2895 [0.259, 0.319]** (−0.032, CIs overlapping), all-rows base 0.4137 → 0.4054. The v2 sentiment fields' add-one is unchanged (+0.060 vs +0.062): they carry x by construction, not through the price loop. Read with E3.8 (process stack 0.203) and E4.21 (residual ≈ 0.118): the observables and the feedback together do not explain the calm channel, which is the engine's. | Attributing the calm level-free residual to the fields (they are outside the level-free base by construction) or to the feedback alone | `e5_arms/sentiment_v2_bpred0/ablation.{json,md}`; `e5_7a/baseline/ablation.json` |
| P5-16 | **The permutation null margin is NEGATIVE (ANALYST p95 -0.0057, SENT -0.0024): the absolute-admissibility clause is mis-specified and is disclosed, not repaired; no decision depends on it** | 20 across-path permutations of the group block on P-all: every draw below zero (ANALYST -0.0110 to -0.0057; SENT -0.0039 to -0.0018) — a permuted block costs the GBT out-of-sample fit, so the null sits at the noise cost of extra columns, not at zero; design C (no information by construction) is at -0.0017 [-0.0046, +0.0014], above it. PP5's synthetic check had put the null at 10⁻⁴. E5.4's rule is conjunctive and every A arm fails the onset clause alone; E5.5's rule (ii) uses paired increments. | Re-specifying the clause after seeing the draws (a centred or interval version is the repair, Phase 6's to register); or reading "no field is inside the margin" as "every field leaks" | `e5_7a/baseline/ablation.json` (`null_margins`); `PREREG_PHASE_5_ADDENDUM.md` section 6 |
| P5-17 | **E5.4: no design-A sd is admissible; design C (F_t = SMA250(P)_t·e^u at the LIT sd 0.564) is the PROVISIONAL default, C-vs-B is Phase 9's, B (drop the field) the fallback** | The rule (PREREG 7.3) is conjunctive. Onset clause: every A arm fails at the crash's calm→deterioration — sd 0.300 +0.021, 0.450 +0.018, 0.564 +0.017, 0.600 +0.018 against a null p95 of +0.001 (v2's sd 0.15: +0.034); the excess is flat across the Phase-9 bracket because V's decline, not the noise, is the leak. Null-margin clause: ΔR²_add(ANALYST) +0.001 to +0.002 at every sd (CIs include zero) against a margin measured at −0.0057 (P5-16). C: ΔR²_add −0.0017 [−0.0046, +0.0014], passes the onset rule at all six transitions (worst −0.037), one registered-rule failure at blow-off→post-top (n = 26) against the narrow reference only. Calm-trained co-statistic for C +0.015 [+0.003, +0.024] (a price-derived field, the technicals' kind of information). The expectation written in PREREG 7.1 is confirmed. | Adopting A at the anchor because it is LIT (the rule makes admissibility, not provenance, decide); dropping the field in this phase (B), which the register reserves for Phase 9 | `e5_arms/analyst_*/ablation.{json,md}`; `e5_7c/analyst_*/onset.json`; `e5_arms/decisions.md`; `envs/v2/params/observables.json` |
| P5-18 | **The whole-tree run (P4-38) found the CI leakage fixture feeding the raw panel to the audit: an undefined P/E's NaN made the `k·P/reported_PE` candidate's APE NaN on 6 % of days, which the L1 share counts as *inside* the floor; the fixture now applies the registered audit-side encoding** | `tests/test_leakage_ci.py::test_v2_L1_no_algebraic_inversion` failed on the Phase-5 state with the P/E candidate at median APE 0.64, within-5 % 0.02 and `share_APE_above_floor` 0.887 against the rule's `price share − 0.01` = 0.896: `np.nanmean(ape > NOISE_FLOOR)` evaluates a NaN comparison as False, so every n/m day is an "inversion inside the floor". The fix is where the pre-registration put the convention (section 10d): the `v2_audit` fixture encodes n/m as the cap plus the `reported_PE_nm` indicator passed as a shown field, exactly as the after-state audit and the ablation tool do (`tools/phase5/common.encode_nm`). `evaluation/leakage_audit.py` is unchanged (not this phase's). | Editing the audit (`nanmean` over finite APEs) — a change to frozen machinery outside the phase's remit; or registering the failure as a known defect | `tests/test_leakage_ci.py`; `PHASE_5_CHANGED_FILES.md` |
| P5-19 | **The final state, measured after the file settled: the observables redesign closes the L2b gate and removes 93 % of what the fields carried about x** | SEP audit on the after panel (1,600 paths, level-free control, n/m as cap + indicator): **L2b selectivity +1.8 pp vs the 10 pp margin → PASS** (+10.4 pp FAIL after Phase 4; full 67.6 %, price-only 65.8 %); the L2 absolute gate passes (calm best R²(x) −0.61, event 0.45, max selectivity +0.028). Final ablation (70 fits): full-field R²(x) on all rows **+0.805 → +0.432** over a level-free base of +0.406 — the eighteen fields add +0.026 where they added +0.39; worst group VAL +0.009 (bound 0.20); calm-trained VAL +0.051 [+0.035, +0.068] is the largest remainder (the wandering multiple is a slow signal). Calm-trained full-field R²(x) 0.4905 → 0.3950 (non-overlapping CIs), level-free 0.3213 → 0.2912 (overlapping). Onset: PASS under the non-price rule at all six transitions. L5: oracle < L5 < trivial everywhere, both gaps' CIs above zero; the oracle→L5 gap widens 0.005–0.009 in three scenarios. Whole test tree: 159 passed, 1 skipped, 4 xfailed (registered). | Reading the calm-trained level-free residual (0.29) as the fields' — it is the process stack's (E3.8) and the feedback's (P5-15), and it is Phase 6's to gate | `e5_after/audit_after_levelfree.md`; `e5_7a/final/ablation.{json,md}`; `e5_after/calm_trained_phase5.md`; `e5_7c/final/onset.json`; `e5_l5/discrimination.md` |
