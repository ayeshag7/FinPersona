# E1–E4 status (23 Aug 2026)

## What exists and is tested
| Phase | Built | Validation |
|---|---|---|
| **E1 core generator** | `envs/v2/*`, facade `envs/synthetic_market.py` (same API as v1 + extras); separate named RNG streams; 260-day burn-in; N-asset-capable V (GBM, t(5), common factor); FW mispricing (`fw_single` fallback, `fw_index`, `pruna`, `ar1`); GJR-GARCH-t with regime-scaled variance; scripted crash/mania/post-top drifts with error correction; hazard top (calibrated: 47% topped, topped peak P/V 2.1); sustained-bull regime; schedule with setup-first / event-first / phase-free orderings and jitter; rejection sampling with logged attempts/reasons; `PortfolioV2` (start design, target-share action, dead band, cost, next-open option, v1 interface for the bridge cell); `TargetAllocation` schema | Section 9 checklist, 50 seeds: **9 pass / 6 fail / 7 n-a** (`generated/checklist_v2.md`; v1 baseline 3/10/7); `tests/test_v2_generator.py` (14), `tests/test_action_space.py` (15, items 18-19) pass |
| **E2 observables** | `envs/v2/observables.py`: quarterly lagged noisy EPS with hidden multiple, trailing-4Q P/E, sticky dividends, analyst fair value (fixed ex-ante error), sentiment AR(1) driven by x and returns with the Tetlock-sized b_pred feedback, log-AR(1) volume driven by \|r\| and \|x\|, IV from the GARCH forecast with a state-based premium, SMA20/50, Wilder RSI, MACD+signal; horizon disclosure and field-order factors; Table 2 generated from code (19 fields, all rendered) | Checklist 7 (margin), 12 pass, 13 (margin); Section 5 audit, 20 seeds (`generated/leakage_audit_v2.md`): **L1 pass (A6), L2 pass on selectivity (A8; absolute numbers reported as failing), L2b pass (+7.5 pp), L4 coverage flat 75% / crash 84% / bull 85% at theta 0.05** |
| **E3 harness** | `agent/v2_agent.py` + `agent/v2_prompts.py`: one configurable agent for static / memory / declarative placebo / directive placebo / wrapper-only / swapped / no-mandate trader (+objective) / Path-B (mandate in system prompt); wording levels (original, rewritten ISFJ clause, paraphrase, abbreviated, no-delimiter, no-action-clauses, numeric-only); Track A/B; cost visibility; horizon disclosure; forked restatement probe; decode replicates; `simulation/runner_v2.py` (per-row referee fields, factors, provenance hashes, parse status); `experiments/arms_v2.py` registry + grid CLI + bridge cell | `tests/test_v2_harness.py` (offline, fake LLM) passes end to end |
| **E4 evaluation** | `evaluation/targets.py` (bands), `metrics_v2.py` (point/band/relative MAS, RG_v1, RG_theta + coverage, action-only RG, **mandate-conditional regret MCR**, return/MDD/turnover/cost, fallback and zero-trade accounting), `baselines_v2.py` (12 rule-based policies inside the env from the cell's start), floors/ceilings/normalisation/beats-k with degeneracy flags, `salience.py` (target-free surrogate with permutation null; t=0 gate), `tools/report_v2.py` (per-run, per-cell, bull-trap strata, reliability, salience, gate tables) | `tests/test_eval_v2.py` passes; RG_v1 normalisation is degenerate by construction (documented) -> MCR is the RG-type headline |
| **E5 stateful arm** | `agent/stateful_agent.py` (rolling-window / full-transcript context as prior turns; mandate in the system prompt at t = 0; `Context_Tokens`, `Mandate_Offset_Tokens`, `Context_Turns` logged per step); arms `stateful_static`, `stateful_memory`, `stateful_full_*` | `tests/test_stateful_multiasset.py` passes (context grows then plateaus; offsets grow) |
| **Multi-asset (3-asset) configuration** | `--three_asset` in `experiments/arms_v2.py` (scenario asset + correlated peer + defensive low-vol asset); sleeve weights from `TargetAllocation.target_weights`; per-asset x/V/weights logged; MCR on the scenario asset, band on total cash (weights oracle deferred) | `tests/test_stateful_multiasset.py::test_three_asset_end_to_end` passes |
| **Statistics track (11.2)** | `tools/stats_v2.py`: model-level sign counts, Cliff's delta / Hedges g with bootstrap CIs, BH across the metric family, mixed-effects (metric ~ arm x persona, random intercept by model, seed variance component), degeneracy audit | `tests/test_stats_v2.py` passes on a synthetic bidirectional effect |
| **E6 draft** | `CALIBRATION_REPORT.md` (parameters in force, checklist, hazard grid, Section 5 tables); FW SMM attempted on real data and rejected (not identified, J = 408) -> documented fallback | |

## Decisions taken on the open issues (23 Aug 2026; for team review — `DECISION_LOG.md`, last section)
1. Checklist 3 (clustering share 60% vs 80%): accept/report; `garch_strong` sensitivity listed for E6.
2. Checklist 10 (delta): statistic amended to event-window MDD (**A7**); still fails (0.38 / 18 pp); reported with the reason (plan's own panic variance).
3. L2 surrogate: gate on selectivity of non-price fields (**A8**: <= 0.20 R2, <= 5 pp MAPE, shuffled ~0; margins frozen with headroom) — passes; absolute thresholds reported as failing by construction (price-only inference).
4. Checklist 17 rejection rates (bull 5.7%, sustained-bull 24%): accept and publish; FW units: keep `price_scale=100`, flagged for verification.
5. Margin items 6/7/13: accept as-is, report; no re-tuning.

## Not done (next)
- Real LLM runs on the v2 harness (E7) — everything runs offline with a fake LLM; needs API keys/providers.
- E5: summarisation-memory arm and the M0-M4 mechanistic anchors (need open-weight models); the rolling/full arms are built.
- 3-asset: per-asset evaluation (weights oracle, per-asset L2/L2b audits) after the N = 1 results exist.
- E6 sensitivities that need LLM runs (cost tier/visibility, start design, interface, field order, horizon, Track A).
- Statistics track: scripted (`tools/stats_v2.py`); runs once E7 data exist.
- If the team reverts A7/A8 or changes a parameter anchor: re-run `evaluation.stylized_facts --env v2 --seeds 50` and `evaluation.leakage_audit --env v2 --seeds 20`.
